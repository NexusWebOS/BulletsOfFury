const fs=require('fs'),path=require('path'),crypto=require('crypto');const dir='output/tempest-jet';let s=fs.readFileSync(dir+'/engine.js','utf8').replace('BULLET HELL • SHOOT DOWN MISSILES','MISSILE PRESSURE').replace('REAR BATTERY • STAY ON HIS SIX','REAR BATTERY');fs.writeFileSync(dir+'/engine.js',s);let h=fs.readFileSync(dir+'/README.md','utf8');h=h.replace('dense aimed spreads and homing missiles','alternating cannon bursts, short aimed spreads, recovery gaps, and warned homing missile salvos');h+='\n## Mounted-cannon revision\n\nFour real armored cannon turrets now sit on fixed hydraulic carriages in the wing sockets. Their bearing pivots follow hull rotation; slide travel stays within 12 pixels, aim turns at 1.65 radians/second, and each firing event drives muzzle flashes and mechanical recoil. The old miniature ship-like turret art is no longer rendered. Bullets use the existing BulletsOfFury pellet sprite. The attack rhythm adapts the game’s forward-cone fire, short fan patterns, and gun/missile alternation, with reload gaps. `sources/bof-behavior-reference.md` records the read-only reference pass. Run `node rig-test.cjs` for mounting and pressure tests. A 30-second heavy-phase run peaked at 12 live projectiles.\n';fs.writeFileSync(dir+'/README.md',h);const m=JSON.parse(fs.readFileSync(dir+'/manifest.json'));for(const n of fs.readdirSync(dir+'/assets')){const b=fs.readFileSync(dir+'/assets/'+n);m.assets[n]={file:'assets/'+n,bytes:b.length,sha256:crypto.createHash('sha256').update(b).digest('hex'),...(n.endsWith('.png')?{width:b.readUInt32BE(16),height:b.readUInt32BE(20)}:{})}}m.rig.turrets='Four fixed hydraulic wing carriages with bounded local rail travel and independent cannon bearing rotation';m.behaviorReference='sources/bof-behavior-reference.md';fs.writeFileSync(dir+'/manifest.json',JSON.stringify(m,null,2));
