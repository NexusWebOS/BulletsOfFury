# Dialogue portraits and Back navigation — 2026-09-20

The nine existing pilot comm portraits were visually inspected. Their side-facing heads all looked left in the source plates; Yuri is nearly frontal. The shared `dlgBox` draw now mirrors each portrait at render time so the pilots face the dialogue on the right. The square grows to fill the available portrait bay below the speaker name, within the authored panel borders. No source art was altered. The Cole and Yuri Campaign bridge captures are `_shots/dialogue_back_0920/cole_portrait.png` and `yuri_portrait.png`.

Campaign's input pre-handler previously consumed B/Back throughout its hub, difficulty, pilot and map states, then did nothing with it except when a map button bar was focused. The setup screens now use the common navigation route: Campaign hub → mode select; Campaign difficulty → hub; pilot → difficulty. Arcade and Co-op difficulty → mode select; pilot → difficulty. On the live Campaign map, Back first leaves the button bar, then returns to the hub. The save/load slot picker still uses Back to cancel the picker. Password difficulty still returns to password.

`_BUILD_SOURCE/probe_dialogue_back_0920.py` ran these paths with keyboard K and the logical controller B (`pad_b1`) in real Chromium. Both dialogue renders were inspected, and there were no page or console errors. `node --check assets/game.js` and whitespace diff check passed; `assets/game.js` remains LF and `_BUILD_SOURCE/test_fl.js` remains CRLF. The menu table assertions were updated for the intended destinations. The first full-suite run exposed four intermittent Stage-1 jet dodge assertions unrelated to these changes; a repeat reached its final summary with exactly the same 76 failing names as the recorded baseline and zero new failures. It still exits 1. Final log: `_shots/test_fl_dialogue_back_repeat_0920.log`.
