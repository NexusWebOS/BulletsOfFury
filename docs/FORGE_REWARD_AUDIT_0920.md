# Forge reward-flow audit — 2026-09-20

`_BUILD_SOURCE/probe_forge_reward_flow_0920.py` drives the real game in Chromium through a shortened Stage-1 boss final blow, its guaranteed Kinetic drop, end-of-stage auto-collection, Forge, Loadout, Stage-2 launch and one player death. The browser probe and captured Forge canvas are in `_shots/forge_reward_flow_0920/`; it reported no page or console errors.

The Stage-1 boss produced exactly one Kinetic element. The Forge opened with two combines, and both could be applied to distinct weapon slots with controller input. A third combine was denied. Re-spec removed the active Spread form while preserving its crafted form, and Loadout restored that form. Audible SFX routes fired for navigation, successful crafting and denied input. The crafted forms and held infusion remained after a Stage-2 death; the ordinary weapon level reset to zero.

This proves the Stage-1-to-2 route exercised in the probe. The fixture starts at the boss's last hit, so it does not establish an unmodified full-stage playthrough. Reward sequences for other bosses and all weapon combinations still require natural-play review before ENG-23 can be marked complete.

The follow-up `_BUILD_SOURCE/probe_forge_campaign_save_0920.py` covered campaign persistence in a fresh Chromium browser context. It wrote a manual slot with two discovered elements, two saved Machine Gun forms, active selections, Loadout and an Ice Breath slot variant; reloaded the page, restored the slot, then wrote an end-of-Stage-2 autosave and reloaded again. The Stage-3 entry reapplied the selected Kinetic element automatically. Both storage round-trips preserved the forms, variant and inventory, and Chromium reported no page or console errors. Results are in `_shots/forge_campaign_save_0920/results.json`. The initial Forge reward fixture still starts at the Stage-1 boss's last hit; later boss rewards and full natural play remain open.

Another Chromium fixture, `_BUILD_SOURCE/probe_later_boss_elements_0920.py`, invoked the real defeat route for the Stage-2 through Stage-6 bosses. Each dropped exactly one stage element: Fire, Ice, Lightning, Chrome, then Dark. No page or console errors occurred; the result is in `_shots/forge_later_boss_0920/probe.log`. This proves the reward routing at defeat, while natural stage progression and the remaining stages still need play review.

`_BUILD_SOURCE/probe_final_boss_elements_0920.py` extended that check to Stages 7–9: Toxic, Prism and Water each dropped once, again without browser errors. Stage 7 starts a staged defeat animation and deliberately keeps its boss object alive during the first frame, so its immediate `dead` flag is not the completion signal. The remaining Forge work is a natural-play pass through the later stages and the actual variety of crafted combinations, not an untested boss reward table.

A subsequent real-game pickup probe found a Forge leak: switching from a level-V Kinetic Machine Gun to a level-I Kinetic Spread left the Spread at level V, and switching again to a bare flamethrower left the Kinetic aura active. `forgeApply()` now tags an aura with its originating weapon slot, applies that slot's exact crafted level on each switch, and clears the aura on a bare weapon. An independent non-Forge infusion keeps its own lifetime. `_BUILD_SOURCE/probe_forge_switch_0920.py` first reproduced both failures, then passed after the repair; it also exercised all 81 combinations of nine weapons and nine stage elements. The boss reward/UI/death and campaign save/autosave Chromium probes passed again with no browser errors. The broad natural-play combination review is still open.

After the independent-infusion guard, `node --check assets/game.js` and `git -c core.whitespace=cr-at-eol diff --check` passed. The full `node _BUILD_SOURCE/test_fl.js` run reached its final summary and exited nonzero with 76 failing assertion names, exactly the same set as `_shots/qa_0920_audio/test_fl_final.log`; no new failing names appeared. The 81-pairing Chromium probe passed again after that final code change.
