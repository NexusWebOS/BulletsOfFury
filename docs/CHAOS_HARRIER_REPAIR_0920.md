# Stage 5 Chaos Harrier repair — 2026-09-20

The Chaos Harrier's teleport rift was kept. Its 3-warp cycle and stationary laser damage window remain the encounter structure; the hull never banks during patrol or attacks.

The supplied four-frame weapon plates pack each pose at a different horizontal offset. The old renderer blitted whole 192/224-pixel plates from a single origin, so the plasma shots, wing lances, muzzle flashes and reactor beam jumped sideways as their frames advanced. The plasma projectile also drew a full square source plate, compressing the long bolt into a stub. The renderer now crops each authored frame at its measured ink location, scales plasma as a long bolt, centers charge/flashes, and keeps the wing emitters fixed to their hardpoints. The central beam's damage width was matched to its visible core.

The arrival weapons now rotate through wing plasma, two sequential straight-lane missiles from the actual left/right open bays, and paired wing lasers. Plasma glides across the player's lane without rolling the hull. After three teleports the Harrier holds position for the existing beam/wing/beam damage window. Both wing flashes can appear together; one no longer overwrites the other. Its spawn and warp anchors were lowered so the hull clears the `MINI BOSS` gauge instead of sliding underneath it.

Verification: `node --check assets/game.js` passed. `python _BUILD_SOURCE/probe_chaos_harrier_0920.py` ran the native game in Chromium through spawn, teleport out/in, plasma, missile, side-laser and nose-beam phases. Both missile lanes remained straight, all relevant authored art loaded, and there were no page or console errors. `_BUILD_SOURCE/shoot.py` captured 30 real-canvas frames of the final fight in `_shots/chaos_harrier_final_full_0920/`; visual review includes open bays, both beam types and the health-bar clearance. The full `node _BUILD_SOURCE/test_fl.js` run still exits 1 with 76 failing assertions, matching the recorded 0920 baseline count. One rerun matched all 76 names exactly; the other two exchanged the unrelated, intermittent sand-tank and three-drone assertions.

The wider Stage 5 field still carries any hazards already on screen and continues its useful pickup supply. A follow-up now holds scheduled and adaptive enemy waves and new asteroid/comet spawns from the first Harrier warning through its defeat, then resumes both streams. Existing rocks remain solid and drift off naturally. New supply containers choose a visible side lane outside the Harrier hull instead of crossing its weapon ports. `_BUILD_SOURCE/probe_chaos_harrier_waves_0920.py` confirmed in Chromium that a due wave and rock stay queued during the fight and dispatch afterward, and ordinary, capsule and missile crates all spawn clear of the hull. The complete attack-cycle probe passed again with no browser errors.

The pinned attack selector also had a one-step indexing error: its documented beam/side/beam sequence played side/beam/beam. It now opens the stationary damage window with the nose beam, follows with the paired wing lasers, and closes with the nose beam. The full-cycle Chromium probe records that exact transition order after the third warp.

The updated real-canvas fight recording is `_shots/chaos_harrier_repaired_0920/capture.gif` (32 frames at 2 fps). The frame capture and probe were rerun after the encounter cleanup, and representative frames were visually inspected.

An ordinary Stage-5 progression probe exposed a gap that the isolated boss fixture could not: nine surviving fleet units, shield domes, purple portal effects and their shots were still on screen when the Harrier appeared. The first natural-spawn screenshot in `_shots/stage5_natural_mini_0920/` showed the boss buried under that wave. The miniboss warning now clears regular fleet units and their active hostile rounds before the teleport entrance, while retaining physical asteroid/comet bodies and pickups. Scheduled waves remain queued until the fight ends. The repeated normal-progression probe reached the Harrier at 28.57 seconds with no regular enemies overlapping it; two physical rocks remained in the field. The final screenshot was inspected and Chromium reported no page or console errors. The full attack-cycle probe also passed again through all three teleports, plasma, straight missiles, wing lasers and nose beam. `node --check assets/game.js` and `git -c core.whitespace=cr-at-eol diff --check` passed. The full suite still exits 1 with exactly the recorded 76 failing assertion names and no new failures.
