
## cutscene-dialogue-window-sizing

### [UNVERIFIED] Cutscene dialogue frame is drawn at an arbitrary, non-uniform scale (per-pilot opening / beats renderer)
- where: game.js:40311
- now: cinDialogue builds its rect from raw viewport fractions with NO reference to the art: `const dx=Math.round(W*.025),dy=Math.round(H*.715),dw=Math.round(W*.95),dh=Math.round(H*.255);` then blits the plate into it with a 6-arg drawImage at line 40324 (`ctx.drawImage(XART.get(_dk),dx,dy,dw,dh)`). W=cutsceneViewWidth()=CINEMA_VW, H=VH=512 (VW/VH declared assets/game.js:14; CINEMA_VW computed assets/game.js:6096 as Math.max(640,Math.round(VH*innerWidth/innerHeight))). So dh is ALWAYS 131px, and dw floats with the browser. On a 16:9 window CINEMA_VW=910, dw=865, dh=131 -> drawn aspect 6.60:1. The source plate dlg_window is 1465x808 = 1.813:1 and the dlg_<pilot> plates are 569x321 = 1.773:1. That is x-scale 865/1465=0.590 against y-scale 131/808=0.162, i.e. the frame is stretched 3.64x wider than tall (3.73x for a pilot plate, which is simultaneously MAGNIFIED 1.52x horizontally and CRUSHED to 0.41x vertically). Even at the narrowest allowed CINEMA_VW=640 the distortion is still 2.56x. This is exactly Mike's 'do not stretch dialogue boxes'.
- FIX: In cinDialogue (assets/game.js:40309-40337), stop deriving dh from H. After `_dk` is resolved at line 40323, read the chosen plate's natural size and size the rect from it: keep dy/dh as the maximum band, compute a uniform k=Math.min(maxW/im.naturalWidth, maxH/im.naturalHeight), set dw=im.naturalWidth*k, dh=im.naturalHeight*k, and centre horizontally (dx=Math.round((W-dw)/2)), bottom-anchoring dy to the old band. Factor this into one small helper (e.g. cinPanelRect(imgKey,W,H)) and call it from here AND from drawCutscene:4012 so both cutscene renderers share it. NOTE FOR THE SPRITECOOK BRIEF: at 1.813:1 an aspect-correct panel filling 95% of a 910-wide cinema view would be 477px tall, i.e. 93% of the 512 screen. The current panels can only span the bottom band because they are stretched. New cinematic dialogue windows must therefore be authored WIDE - roughly 4:1 to 6.5:1 (e.g. 1280x320 or 1600x256) - or the helper will letterbox them into a much smaller centred box.
- risk: The interior text insets in this function are FRACTIONS of dw/dh (line 40325 onward), so they follow the frame automatically and will not break when dw/dh change. But if the existing 1.8:1 art is kept, an aspect-correct panel becomes far taller and will cover the pilots' ships - so this fix should land together with the new wide SpriteCook plates, not before them.

### [UNVERIFIED] The ensemble / room cutscene renderer repeats the identical stretched rect
- where: game.js:4012
- now: drawCutscene has a byte-identical copy of the same maths one line before its frame blit: `const dx=Math.round(CW*.025), dy=Math.round(CH*.715), dw=Math.round(CW*.95), dh=Math.round(CH*.255);` where CW=cutsceneViewWidth() and CH=VH. The plate is then drawn with the same 6-arg stretch at line 4019 (`ctx.drawImage(XART.get('dlg_window'), dx, dy, dw, dh)`). Same 3.64x horizontal distortion as cinDialogue. Note that the surrounding function is otherwise scrupulous about uniform scale - every portrait uses `Math.min(mw/im.naturalWidth, mh/im.naturalHeight)` (lines 4001 and 4021-ish in the slots loop) and the background uses a Math.max cover with matched sx/sy. The dialogue frame is the ONE element in drawCutscene that ignores its source dimensions.
- FIX: Replace lines 4012 and 4018-4019 with the same cinPanelRect helper proposed for cinDialogue, so the room/ensemble cutscenes and the per-pilot openings size the panel identically. Do not duplicate the maths a third time.
- risk: drawCutscene's text layout at lines 4057 and 4080-4081 uses DLGW_IN_X/W/Y/H (declared line 3879), which are fractions measured off dlg_window (1465x808). Those are fractional and so survive a change of dw/dh - but they are dlg_window's interior specifically, so if the frame key becomes a pilot plate (see next finding) the insets must switch too.

### [UNVERIFIED] The ensemble cutscene renderer still hardcodes the bare silver dlg_window - the 0905 'white slab' fix was applied to cinDialogue only
- where: game.js:4018
- now: Line 4018-4019 asks for `'dlg_window'` unconditionally: `if(typeof XART!=='undefined' && XART.rdy('dlg_window')){ ctx.drawImage(XART.get('dlg_window'), dx, dy, dw, dh); }`. The speaker is not resolved until the NEXT line, 4024: `const spk=sc[sc.speaking||'left'];` - and spk.pilot is a valid pilot key, proven by line 4051 which does `const ek='pemb_'+spk.pilot;`. cinDialogue was fixed for exactly this at line 40323 (`const _dk=(_who && XART.rdy('dlg_'+_who)) ? ('dlg_'+_who) : 'dlg_window';`) and its own comment at 40312-40322 records Mike's complaint verbatim - 'fix white in all cutscenes' - and measures the bare panel at 1.08% of the frame in near-white across rows y 736..968. The ensemble/room path was never given the same treatment, so every HQ briefing still shows that white slab.
- FIX: In drawCutscene, hoist `const spk=sc[sc.speaking||'left'];` from line 4024 to above line 4018, then mirror cinDialogue:40323 exactly - `const _dk=(spk&&spk.pilot&&XART.rdy('dlg_'+spk.pilot))?('dlg_'+spk.pilot):'dlg_window';` - and draw _dk instead of the literal. Two-line change, no new state.
- risk: The DLGW_IN_* insets used at lines 4057 and 4080-4081 were measured off dlg_window. drawCommWindow:51547-51548 already records the correct pair for dedicated plates (0.075 / 0.16 / 0.855 / 0.66) versus generic (0.055 / 0.10 / 0.89 / 0.72), so the same ternary should gate the insets here or the text will sit slightly off on pilot plates.

### [UNVERIFIED] cinDialogue applies the GENERIC frame's text insets to dedicated pilot plates
- where: game.js:40325
- now: `const padX=dw*.055,nameY=dy+dh*.115,nameH=Math.max(12,Math.round(H*.030));` - the 0.055 is dlg_window's generic inset, and it is used unconditionally even though line 40323 may have selected a dlg_<pilot> plate. drawCommWindow, the reference implementation, explicitly distinguishes the two at assets/game.js:51547-51548: inL uses 0.075 for a dedicated plate versus 0.055 for the generic window, inT 0.16 vs 0.10, inW 0.855 vs 0.89, inH 0.66 vs 0.72 - with the comment at 51546 saying those were measured on the dedicated frame art. Since the 0905 fix made cinDialogue draw pilot plates for the first time, its insets became wrong for the common case: the dedicated frames have a deeper machined border (0.075 vs 0.055 left, and 0.66 vs 0.72 usable height), so name and body now sit ~2% of panel width out over the left rail and the body block is given ~9% more vertical room than the plate actually has.
- FIX: In cinDialogue, mirror drawCommWindow's ternary. Add `const _ded=(_dk!=='dlg_window');` next to line 40323, then make padX / nameY / bodyY / bodyH read from that: padX=dw*(_ded?0.075:0.055), and scale bodyY/bodyH (currently dh*.34 and dh*.52 at line 40328) off the 0.16/0.66 vs 0.10/0.72 interior instead of the current hardcoded fractions. Do not invent new numbers - reuse the four pairs already measured at 51547-51548.

### [UNVERIFIED] drawCommWindow - the only aspect-correct path - still squashes the generic dlg_window by a magic 0.52
- where: game.js:51513
- now: `const ph=_dedicated ? Math.round(pw*(XART.get(_fk).naturalHeight/XART.get(_fk).naturalWidth)) : Math.round(pw*0.52);` with pw fixed at line 51512 as `Math.min(VW-24,446)` = 446 (VW=480). For a dedicated dlg_<pilot> plate this is genuinely uniform - ph = round(446 * 321/569) = 252, and 446/252 = 1.770 matches the art's 1.773 exactly. This is the correct model and it is why the rival mid-stage dialogue (assets/game.js:55487) and the pilot-select 'GOOD LUCK, PILOT!' window (assets/game.js:51639) look right while the cutscenes do not. But the generic branch uses a hardcoded 0.52 instead of dlg_window's true 808/1465 = 0.5515, giving ph = 232 where aspect-correct is 246 - a 5.7% vertical squash on the generic panel. Small, but it is a real non-uniform scale and the last magic number in the otherwise-clean path.
- FIX: Delete the ternary and use the natural ratio for both branches: `const ph=Math.round(pw*(XART.get(_fk).naturalHeight/XART.get(_fk).naturalWidth));` - guarded by the existing XART.rdy(_fk) check at line 51517, with the 0.52 kept only inside the else-branch fallback that runs before the art decodes.
- risk: ph grows from 232 to 246 (+14px). py0 is VH*0.34 = 174 (line 51514), so the panel bottom moves 174+232=406 -> 420 against VH=512. Still clears the screen, but the 'PRESS FIRE / ENTER' prompt drawn at VH*0.34-24 (line 55490) is above the panel and unaffected.

### [UNVERIFIED] Source art natural dimensions - where they are declared
- where: manifest.js:4
- now: The dialogue plates are atlas cells, not standalone files. window.BOFX.cells maps key -> [sheet,x,y,w,h] against the sheet assets/game/atlas/ui_dialogue.png. Measured values: dlg_window ['ui_dialogue',0,0,1465,808] = 1465x808, aspect 1.8131; dlg_cole 570x322 (1.770); dlg_decker 570x321 (1.776); dlg_axel / dlg_freezer / dlg_yuri / dlg_falva / dlg_lizzie all 569x321 (1.773); dlg_juggernaut 552x322 (1.714); dlg_maverick 552x321 (1.720). dlg_falva additionally has a standalone override registered at assets/game.js:1917 (`X._src['dlg_falva']='assets/game/ui/dialogue/dlg_falva.png'`, which also deletes the cell on the next line) - I read that PNG's IHDR directly and it is 569x321, so the override matches the cell and introduces no aspect change. The 1465x808 figure is independently corroborated by the measurement comment at assets/game.js:3865.
- FIX: No code change. Use these as the ground truth for the SpriteCook brief: the existing family is ~1.77:1 for pilot frames and 1.81:1 for the generic. New cinematic windows need to be a deliberately different, much wider aspect (see finding 1) and should be delivered as their own keys (e.g. cin_dlg_<pilot>) rather than replacing dlg_<pilot>, because dlg_<pilot> is shared with pilot select and the rival mid-stage dialogue, which are already correct and must not change shape.

### [UNVERIFIED] Other non-uniform blits of the same plate outside the cutscene path (for the 'clean up all frames' sweep)
- where: game.js:39714
- now: The same 6-arg stretch of dlg_window appears on several non-cutscene surfaces, all ignoring the 1.813:1 natural aspect. The arcade pilot-intro 'PILOT DEPLOYED' window at line 39714 draws it into AIN_BOT, declared at line 39688 as [325,338,302,120] in 640x480 plate space = 2.517:1, a 1.39x horizontal stretch. Worse: drawCampSlots at line 49943 draws it at w=330,h=42 (line 49934) = 7.857:1, a 4.33x horizontal stretch; campPauseDraw at line 39396 draws it as slot buttons roughly 424x78 = 5.44:1, a 3.0x stretch; and the in-play dlgBox at line 32445 uses pw~344 by ph=round(VH*0.17)=87 (lines 32440-32441) = 3.95:1, a 2.18x stretch. These are outside the cutscene complaint but are the same defect and will be visible next to any newly-corrected cinematic panel.
- FIX: Out of scope for the cutscene fix - do not touch them in the same change. If the cinPanelRect helper from finding 1 is written generically (takes a max box, returns a uniform-fit rect), these five call sites can each be converted later one at a time, each needing its own layout re-check because their text insets are tuned to the current stretched boxes.
- risk: Each of these has hand-tuned text positions inside the stretched box; changing the frame aspect without re-checking the labels will move text off the panel. Treat as five separate follow-ups, not one sweep.

## Cutscene pacing — every timing constant that governs how fast cinematic text and frames go by (HQ per-pilot opening beats, HQ ensemble scenes, campaign intro, victory ending)

### [UNVERIFIED] Typewriter speed (the one Mike named): HQ_CPS
- where: game.js:40036
- now: `const HQ_CPS=42;  // characters per second` — the single type-on rate for BOTH cutscene renderers. Consumed at line 40547 (`hqChars=Math.min(n,hqChars+HQ_CPS*dt)`, beats/pilot-opening path) and line 40611 (`hqChars+=HQ_CPS*dt`, ensemble path). 42 cps is ~500 wpm of streaming text. Measured against the actual scripts: the longest PILOT_OPENINGS line is 224 chars (Freezer, line 39810) = 5.3 s of typing; the longest HQ_SCENES ensemble line is 118 chars = 2.8 s. The blip SFX is tied to this too — every 3rd character (lines 40548, 40613), so at 42 cps it ticks 14x/second.
- FIX: Change the literal on line 40036 from 42 to 24 (comfortable read-along is 22-28 cps; 24 puts the 224-char line at 9.3 s and the 118-char line at 4.9 s). Nothing else needs touching — both call sites already scale by dt, and the every-3rd-character blip drops to a sparse 8/s tick, which is closer to a comic panel than the current buzz. If 24 reads sluggish on the short lines, 26 is the upper bound I'd accept.
- risk: Low. Both consumers are `x += HQ_CPS*dt` with a Math.min clamp; no other code reads HQ_CPS. Only knock-on is the blip cadence, which improves.

### [UNVERIFIED] Ensemble renderer has NO per-line input lockout — the guard is scene-scoped, not line-scoped
- where: game.js:40631
- now: `if(stateT>0.15 && (Input.mouse.down || anyTap())){` in drawCutsceneEnsemble. `stateT` is the STATE timer — it is zeroed only by setState (line 25520), never per line; the per-line advance at line 40639 resets `hqChars` and calls hqSeat but touches no timer. So the 0.15 s protection applies once, to the whole 5-8 line scene. From line 2 onward there is zero minimum display time: two taps 80 ms apart complete the line and advance it. `hqBeatT` is reset by hqPlay (line 40356) but drawCutsceneEnsemble never increments it — it is dead in this path. This is the single biggest reason an ensemble scene can vanish before it is read.
- FIX: In drawCutsceneEnsemble: add `hqBeatT+=dt;` immediately after the `if(!ln){ hqEnd(); return; }` guard (line 40567), add `hqBeatT=0;` beside the existing `hqChars=0; hqSeat(hqLine);` on line 40639, and change line 40631's guard from `stateT>0.15` to `hqBeatT>0.55`. Three lines, no new state — hqBeatT already exists and is already zeroed by hqPlay.
- risk: Low. hqBeatT is only otherwise read by the beats renderer and by cinDialogue's NEXT blink, neither of which runs in the ensemble path.

### [UNVERIFIED] No dwell/hold after a line finishes typing — in either renderer
- where: game.js:40621
- now: There is no hold constant anywhere in the cutscene code. In the ensemble path the moment `hqChars>=n` the NEXT prompt starts blinking (line 40621) and the very next tap advances. In the beats path a dialogue beat is likewise input-only. The game already HAS the constant Mike is asking for, but only for in-mission radio: `STORY_HOLD = 1.35` at line 32298, applied at line 32345 as `wait = (S.when==='safe') ? STORY_HOLD*1.5 : STORY_HOLD`. The cutscenes never got the equivalent.
- FIX: Add `const HQ_HOLD=0.8;` next to HQ_CPS on line 40036, and gate the advance (not the line-complete tap) on it. Surgical form: in drawCutsceneEnsemble line 40631's else-branch, require `hqBeatT >= (n/HQ_CPS)+HQ_HOLD` before incrementing hqLine (with the finding above supplying hqBeatT); in drawCutsceneBeats line 40558, change `else hqAdvance();` to `else if(hqBeatT>=(n/HQ_CPS)+HQ_HOLD) hqAdvance();`. The first tap still snaps the text complete instantly, so it never feels unresponsive — it only stops the second tap from eating the line.
- risk: A hold that blocks input can read as a dead button. Keep it under 1.0 s, and keep the first tap (text-complete) ungated as described.

### [UNVERIFIED] Beats renderer per-beat input lockout: 0.18 s
- where: game.js:40557
- now: `if(hqBeatT>.18&&(Input.mouse.down||anyTap())){ if(text&&hqChars<n)hqChars=n; else hqAdvance(); ... }`. Unlike the ensemble path this one IS per-beat (hqAdvance zeroes hqBeatT at line 40349), but 0.18 s is far too short to survive a player still mashing out of a stage transition. Two taps inside a third of a second take a whole beat.
- FIX: Raise the `.18` literal on line 40557 to `.55`. One number.

### [UNVERIFIED] Auto-advance vs wait-for-input: the split is by beat type, and it is undocumented
- where: game.js:40544
- now: `const dur=beat.duration||0; if(!text&&dur>0){ ... if(hqBeatT>=dur){hqAdvance();return;} }`. So in the pilot-opening path: a beat WITH text waits for input forever (no timeout); a beat with NO text auto-advances on its `duration`. The ensemble path (HQ_SCENES) has no duration field at all and is 100% wait-for-input. Net effect on Mike's complaint: the *dialogue* is not what auto-runs — it is the action frames (the shootdowns, the approaches) that flick past, plus the campaign intro and the ending, which are pure timeline (findings below).
- FIX: No change to the mechanism — wait-for-input on dialogue is correct and should stay. Report this to Mike as the answer to 'auto-advance vs wait-for-input': dialogue holds, action frames do not, and the action frames are the short ones.

### [UNVERIFIED] Action-beat durations — the frames Mike wants to see the enemies in
- where: game.js:39807
- now: Every text-free beat in PILOT_OPENINGS, with its `duration` in seconds: aftermath 1.55 (39807, 39814); ram 1.75 (39850); cloak 1.75 (39867); barrel 1.85 (39824); joyride 1.95 (39843); duo_attack 2.10 (39829, 39858); dogfight 2.15 (39822); missile_lock 2.35 (39837); jungle_approach 3.15 (39819, 39834, 39864); hq_approach 3.75 (39811, 39826, 39840, 39846, 39855, 39874). Each of those spans is also the easing window — cinDrawMotion reads `beat.duration` for its cinClose smoothstep (lines 40232, 40243, 40253, 40272, 40289, 40297) — so the hostile closing on camera, the airburst timing and the approach scaling all complete inside it. The shortest, the 1.55 s aftermath, is the very first frame of the campaign and the one that is meant to sell 'what just happened to us'.
- FIX: Multiply the combat/aftermath durations by ~1.6 in the data table only: aftermath 1.55->2.6, ram 1.75->2.8, cloak 1.75->2.6, barrel 1.85->2.8, joyride 1.95->2.8, duo_attack 2.10->3.4, dogfight 2.15->3.4, missile_lock 2.35->3.6. Leave the two approach beats (3.15 / 3.75) alone — they are already the long ones and they land on HQ. The cinClose easing follows automatically because it reads beat.duration.
- risk: The hardcoded fallbacks in cinDrawMotion (`beat.duration||2.15` etc.) still carry the OLD values; they only fire when duration is absent, which is exactly the dialogue-beat case covered in the next finding. Worth updating them in the same pass for consistency, but not required.

### [UNVERIFIED] During a dialogue beat the ships freeze — the motion easing has no duration to run against
- where: game.js:40206
- now: `const near=cinClose(t,beat.duration||2.4,0,1).e;` in cinFormation. A dialogue beat has no `duration` field, so the easing uses the 2.4 s fallback and cinClose clamps `u=clamp(t/dur,0,1)` (line 40181) at 1. A wait-for-input dialogue beat routinely sits on screen 6-12 s; after 2.4 s the depth breathe, the speaker's push toward camera and the vertical drift are all pinned at their end value and the frame is dead but for the 5px sine bob. Same clamp affects the dogfight/duo_attack/missile_lock/ram fallbacks. Directly relevant to 'clean up all frames'.
- FIX: In cinFormation line 40206, when the beat has no duration, drive the depth from a looping phase rather than a clamped ramp — e.g. `const near = beat.duration ? cinClose(t,beat.duration,0,1).e : 0.5+0.5*Math.sin(t*0.42);`. One expression, no new art, no data change.
- risk: Only affects text beats (the ones with no duration); duration-bearing action beats keep the exact easing they have.

### [UNVERIFIED] Fade between beats — 0.24 s in / 0.34 s out, and ONLY on text-free beats
- where: game.js:40553
- now: `const fadeIn=clamp(1-hqBeatT/.24,0,1),fadeOut=clamp((hqBeatT-(dur-.34))/.34,0,1),fa=Math.max(fadeIn,fadeOut);` — a black wipe, 0.24 s in and 0.34 s out. It is inside the `if(!text&&dur>0)` block (line 40544), so it runs on action beats only: every DIALOGUE beat cuts hard, with no transition whatsoever. The letterbox bars on action beats (line 40543, `Math.max(10,Math.round(H*.052))`) also pop on and off instantly with no ramp.
- FIX: Lengthen to `.40` in and `.50` out on line 40553, and lift the fade out of the `!text` block so dialogue beats get the same 0.40 s fade-in — move the fadeIn computation above the `if(!text&&dur>0)` gate and draw it unconditionally, keeping fadeOut inside the gate (a wait-for-input beat has no known end to fade toward).
- risk: Moving the fade above the gate means it now paints on dialogue beats; verify it draws BEFORE cinDialogue (line 40549) or the black will sit over the panel, since the current call order is backdrop -> motion -> dialogue -> fade.

### [UNVERIFIED] No transition at all between ensemble lines
- where: game.js:40639
- now: `hqChars=0; hqSeat(hqLine);` — the entire line change. No fade, no panel wipe, no beat. drawCutsceneEnsemble contains no fade or transition constant of any kind; the portrait swaps and the text restarts on the same frame. Grepped the whole function (40562-40646): no alpha ramp, no timer.
- FIX: With hqBeatT wired up (finding 2), gate a short black or panel-alpha ramp on it: after line 40588's `drawCutscene(sc)`, `const _f=clamp(1-hqBeatT/0.30,0,1); if(_f>0){ctx.fillStyle='rgba(0,0,0,'+_f.toFixed(3)+')';ctx.fillRect(0,0,_cutW,VH);}`. Two lines. Depends on finding 2 landing first.
- risk: Purely additive draw; worst case is a flash if hqBeatT is not actually reset per line.

### [UNVERIFIED] Campaign intro is a hard timeline with no wait-for-input — the worst reading rate in the build
- where: game.js:40398
- now: CAMPAIGN_INTRO_BEATS (40398-40413) is seven beats with absolute `at`/`to` seconds: 0->3.5, 3.5->6.8, 6.8->10.1, 10.1->13.6, 13.6->17.0, 17.0->20.6, 20.6->24.6. Then CAMPAIGN_INTRO_FINALE=24.6, CAMPAIGN_INTRO_DONE=28.7 (line 40414). drawCampaignIntro (40516-40529) picks the beat by wall-clock t; input only SKIPS the whole thing (line 40528), it never holds. Caption alpha ramps in over 0.28 s and out over 0.32 s (line 40523), so a 3.3 s beat gives ~2.7 s of readable time. Body lengths measured from the table: 72, 118, 124, 104, 101, 123, 125 characters — plus a heading each. Beat 3 is 124 chars in 2.7 s = 46 characters per second of reading demand. Comfortable is 14-17. This screen is roughly 3x too fast and there is no way to pause it.
- FIX: Minimal: rescale the table on lines 40398-40414 by ~2.2x, keeping the beats contiguous — at/to become 0, 7.7, 15.0, 22.3, 30.0, 37.5, 45.4, 54.2, with CAMPAIGN_INTRO_FINALE=54.2 and CAMPAIGN_INTRO_DONE=58.3. Only the numbers change; drawCampaignIntro reads them and needs no edit. Better (and what I'd actually recommend to Mike, since a 54 s unskippable-feeling intro is its own problem): give this screen the same type-on + wait-for-input the cutscenes have, so it is paced by the reader rather than the clock — but that is a behaviour change, not a constant change, so flagging rather than proposing.
- risk: CAMPAIGN_INTRO_FINALE and CAMPAIGN_INTRO_DONE must move with the table or the last beat is cut off / the finale plays over it. campaignIntroFinale's own internal offsets (fireAt=.62, step=.13 at line 40490, and the 1.18/1.45/3.18 ramps) are relative to q=t-FINALE and do NOT need rescaling.

### [UNVERIFIED] Campaign intro crossfade and caption ramps
- where: game.js:40518
- now: `const B=CAMPAIGN_INTRO_BEATS[i],local=t-B.at,fade=.42;` — a 0.42 s crossfade into the next plate (lines 40520-40522), plus the caption's own 0.28 s in / 0.32 s out on line 40523. The caption fade is nested INSIDE the beat window, so it eats 0.6 s off every already-too-short beat.
- FIX: Raise `fade=.42` to `.65` on line 40518 for a softer plate change. Leave the 0.28/0.32 caption ramps alone once the beat windows are lengthened — at 7 s beats they cost proportionally nothing.

### [UNVERIFIED] Victory/ending cinematic act boundaries
- where: game.js:57289
- now: `const VICTORY_DISH_AT=8.4, VICTORY_WARNING_AT=15.8, VICTORY_SPACE_AT=22.8, VICTORY_STINGER_AT=29.3, VICTORY_CARD_AT=32.6;` — absolute seconds against drawVictory._t (dispatched at lines 57441-57446). Pure timeline: the only input is the return-to-title at line 57448, gated on `t>VICTORY_CARD_AT+5`. Nothing in the ending can be held or advanced by the player. Act lengths are therefore 8.4 / 7.4 / 7.0 / 6.5 / 3.3 s.
- FIX: The act lengths themselves are defensible; the captions inside them are not (next finding). If Mike wants the ending slower overall, scale all five by 1.25: 10.5, 19.8, 28.5, 36.6, 40.8 — but scale the caption windows in the same pass or the text just sits longer against a finished animation.
- risk: The per-act functions key off `q = t - <ACT>_AT` and carry their own internal offsets (victoryDish's 4.75/4.25/6.75, victoryStinger's 0.38/1.06/1.10/2.12); those are act-relative and must NOT be scaled, only the five boundary constants.

### [UNVERIFIED] Victory caption windows — same too-fast problem as the campaign intro
- where: game.js:57368
- now: Three hardcoded caption windows, all with 0.28-0.34 s alpha ramps eaten out of them: victoryWarning line 57368 `const second=q>=3.15,local=second?q-3.15:q,dur=second?3.85:3.15;` — the Space Federation line is 104 chars in 3.85 s (~3.2 s visible = 32 c/s); victoryDish line 57356 `if(q<4.55)` for a 70-char body plus heading; victorySpaceLaunch line 57388 `if(q<2.7)` for a 58-char body, with its own 0.3/0.35 ramps, leaving ~2.05 s. The pilot's own ending caption (line 57334) is the one that gets fair time — it runs the full 8.4 s of act one.
- FIX: Line 57368: change `3.15` to `4.6` in both places and `3.85` to `5.6` (and raise VICTORY_SPACE_AT from 22.8 to 25.0 on line 57289 so the act still fits). Line 57388: change both `2.7` literals to `4.0`. Line 57356: change `4.55` to `5.6` in both places. All are literal swaps inside their own act.
- risk: Each window must stay inside its act's length or the caption is clipped by the next act taking over — check each against the boundary constants on line 57289 after editing.

### [UNVERIFIED] NEXT prompt blink rate (minor)
- where: game.js:40621
- now: `if(hqChars>=n && Math.floor(stateT*2.2)%2)` in the ensemble renderer, and the identical `Math.floor(hqBeatT*2.2)%2` in cinDialogue at line 40332. 2.2 gives a 1.1 Hz blink. Note the ensemble one keys off stateT, so across a scene the blink phase is continuous rather than restarting per line — cosmetically fine, but it means the prompt can appear mid-blink-off the instant a line completes.
- FIX: Leave the rate. If finding 2 lands, change line 40621's `stateT` to `hqBeatT` so the prompt always fades up cleanly on the ON half when a line finishes. One identifier.

## Cutscene frame composition — backdrop, bars, dialogue-window placement, and where a comic panel/gutter treatment must be inserted

### [UNVERIFIED] Backdrop is hard-wired edge-to-edge — there is no destination rect to draw a panel into
- where: game.js:40118
- now: cinCover(key,W,H,pan) cover-fits the plate and blits it with `ctx.drawImage(im,sx,sy,sw,sh,0,0,W,H)` at line 40123 — the destination is literally 0,0,W,H, the whole frame. cinBackdrop (line 40130) does the same for its procedural sky/space gradients: `ctx.fillStyle=g; ctx.fillRect(0,0,W,H)` at line 40140. Every cinematic in the game (per-pilot openings, HQ beats, campaign intro at 40468, and all five victory-ending beats at 57337/57357/57379/57391/57408) routes its background through one of these two, so no cutscene anywhere reserves a single pixel of margin for a border or gutter.
- FIX: Add an optional destination-rect argument to both: `cinCover(key,W,H,pan,R)` and `cinBackdrop(kind,t,W,H,R)` where R defaults to {x:0,y:0,w:W,h:H}. In cinCover swap the four literals in the drawImage at 40123 for R.x/R.y/R.w/R.h; in cinBackdrop pass R into the gradient construction and the fillRect at 40140 (and the cloud/starfield blits below it, which key off W/H). No caller changes — existing calls keep the full-frame default — so this lands inert and gives the panel router something to aim at.
- risk: low

### [UNVERIFIED] The ONLY bars in the entire cutscene system, and they appear on silent beats only
- where: game.js:40559
- now: One line in drawCutsceneBeats: `if(!beat.text){const bar=Math.max(10,Math.round(H*.052));ctx.fillStyle='#000';ctx.fillRect(0,0,W,bar);ctx.fillRect(0,H-bar,W,bar);}`. At VH=512 that is a 27px black bar top and bottom (5.2% each). The comment above it at 40558 calls this "a real cinematic aperture". It is gated on `!beat.text`, so an action/travel beat has bars and a dialogue beat has NONE — the frame furniture changes shape every time a beat starts or stops speaking, which is a large part of why the cutscenes read as unclean. Searching the whole file for this pattern returns this line and nothing else: there are no side bars, no border, no second aperture anywhere in any cutscene.
- FIX: This single line is the natural insertion point for the whole panel treatment. In drawCutsceneBeats, replace it with an unconditional call to one new helper — `cinPanelFrame(W,H)` — that paints all four gutters at one width (keep .052 of H for top/bottom so the existing silent beats look unchanged, and use the same absolute px for left/right so the gutter reads square). Drop the `!beat.text` guard so dialogue and action beats carry identical furniture.
- risk: low

### [UNVERIFIED] A two-panel split already exists at H*.70 — it just has no border drawn at the seam
- where: game.js:40557
- now: `cinBackdrop(bg,hqBeatT,W,H); cinDrawMotion(hqSc,beat,hqBeatT,W,H*(beat.text?.70:.94));` — on a talking beat the ships are staged inside the top 70% of the frame, while the dialogue window occupies 0.715H..0.970H (rect at line 40326). So the frame is ALREADY two stacked regions separated by a 1.5%-of-H dead band. The backdrop is painted continuously across both at full W/H, so the seam is completely invisible and the ships appear to float behind the panel rather than inside their own frame.
- FIX: No coordinate changes. Paint the gutter at the seam that already exists: inside the cinPanelFrame helper from the previous finding, add `ctx.fillRect(0,H*.70,W,H*.015)` on text beats. Paint-only; not one beat is restaged and cinDrawMotion's H argument is untouched.
- risk: low

### [UNVERIFIED] The dialogue window is stretched 3.6x horizontally — this is Mike's "do not stretch dialogue boxes", literally
- where: game.js:40339
- now: cinDialogue sets its rect at line 40326 — `dx=W*.025, dy=H*.715, dw=W*.95, dh=H*.255` — then blits the whole authored panel with ONE call: `ctx.drawImage(XART.get(_dk),dx,dy,dw,dh)`. dlg_window's atlas slice is ["ui_dialogue",0,0,1465,808] (assets/manifest.js) → aspect 1.813. The cinematic viewport is CINEMA_VW = max(640, round(VH*ar)) (line 40096 region → line 6096), so on a 16:9 window W≈910 and the destination is 865x131 → aspect 6.60. That is a 3.64x horizontal smear of a machined metal frame: the rivets, the bevelled rails and the star medallion the code's own comment at 3862 describes are all elongated. The per-pilot plates are the same shape of problem — dlg_cole is 570x322, aspect 1.77. There is no nine-slice helper anywhere in the file; drawPanel (line 39299) is also a plain single-call stretch.
- FIX: Nine-slice cinDialogue's blit only. Replace the single drawImage at 40339 with nine drawImage calls, using the frame insets already measured and stored at line 3879 (`DLGW_IN_X=0.0389`, `DLGW_IN_Y=0.0817`) as the corner sizes: corners drawn 1:1, top/bottom edges stretched on X only, left/right edges on Y only, centre stretched freely. Same dx/dy/dw/dh, so text layout at 40340-40348 needs no change. Best done as a small `drawPanel9(key,dx,dy,dw,dh)` next to drawPanel at 39299 so the ensemble path (next finding) can reuse it.
- risk: low

### [UNVERIFIED] The ensemble cutscene path repeats the identical stretched box
- where: game.js:4019
- now: drawCutscene (the HQ ensemble/room path, line 3894) computes the same rect at line 4012 — `dx=CW*.025, dy=CH*.715, dw=CW*.95, dh=CH*.255` — and blits dlg_window with the same unsliced `ctx.drawImage(XART.get('dlg_window'), dx, dy, dw, dh)`. Two independent renderers, one identical defect. Line 4022 is the only strokeRect in either cutscene path and it is dead in practice — the comment at 4017 says it exists purely as the pre-decode fallback.
- FIX: Point line 4019 at the same drawPanel9 helper. Do not touch the fallback branch at 4021-4022. A third copy of this rect also exists at line 39729 (campaign pause), out of scope here — leave it.
- risk: low

### [UNVERIFIED] Campaign intro and the whole ending have no caption BOX at all — just a soft black gradient scrim
- where: game.js:40487
- now: campaignIntroCaption (line 40483) is the narration renderer for both the campaign prologue and — via victoryCaption at line 57343, which forwards straight into it — every beat of the ending. Its box is `ctx.createLinearGradient(0,y-34,0,H)` with stops rgba(0,0,0,0) → .78 → .95, filled full-width across the bottom 23.5% of H plus 34px (line 40488). Head text goes in at '#ffe082' (yellow) on line 40490, body in DIALOGUE_BODY_COLOR ('#f7f9ff', line 3812) on 40491. So the narration furniture is a soft feathered vignette-scrim with no edge, no border and no corner — the exact opposite of the hard-ruled yellow caption box in Mike's Spider-Man strip reference. The head colour is already the right yellow; it is the container that is missing.
- FIX: Inside campaignIntroCaption, replace the gradient+fillRect at 40487-40488 with a hard-edged filled rect plus a black rule: fill x..x+w / y..y+h with a flat caption yellow, then strokeRect the same rect in black at 2-3px. Keep x/w/h/y from line 40485 and both msg calls untouched, and flip the head/body colours to dark-on-yellow. One function, and the ending inherits it for free through victoryCaption.
- risk: low

### [UNVERIFIED] No vignette, no border, no frame and no gutter code exists anywhere in the cutscene renderer
- where: game.js:4022
- now: Exhaustive check of the visual furniture actually present: createRadialGradient occurs 7 times file-wide (22956, 30256, 30297, 31887, 35243, 51415, 55801) and not one is in a cutscene function — there is no vignette. Grep for cinPanel/comicPanel/gutter/drawGutter returns nothing — no panel system was ever started. Of 46 strokeRect calls file-wide, the only one reachable from a cutscene is this pre-decode fallback at 4022. drawScene (line 39463) dispatches GS.CUTSCENE / GS.CAMPAIGNINTRO / GS.VICTORY straight to their draw functions with no post-pass overlay, so nothing paints a frame after the fact either. The complete inventory today is: full-bleed backdrop, ships/FX, 27px top+bottom bars on silent HQ beats only, a stretched dialogue plate, a gradient scrim for narration, and two prompts ('NEXT' at 40347, 'PRESS ANY BUTTON TO SKIP' at 40541).
- FIX: No change to this line. Recorded so the panel work is understood as net-new: cinPanelFrame has no existing code to reconcile with and cannot regress a border, because there is none.
- risk: low

### [UNVERIFIED] The ensemble path already leaves a 113px dead margin per side — the outer gutter has somewhere to live
- where: game.js:3921
- now: `const s=Math.min(CW/SW, CH/SH), ox=(CW-SW*s)/2, oy=(CH-SH*s)/2;` — a CONTAIN fit of the authored 640x480 stage into the cinematic viewport, deliberate per the long comment at 3908-3920. At CW≈910/CH=512 that gives s=1.067, ox≈113, oy=0. But only the portraits go through X()/Y(): the background is drawn cover-fit to 0,0,CW,CH (line 3945 region) and the dialogue box spans CW*.025..CW*.975 (line 4012). So the cast is already confined to a centred 683px column with 113px of unclaimed background on each side, while the backdrop and the dialogue plate ignore that column entirely — an accidental inconsistency, and also exactly where a left/right gutter belongs.
- FIX: When the panel treatment lands, set the ensemble path's outer gutter to ox rather than inventing a new number, so the border falls on the boundary the portrait staging already respects. Read-only observation for now; no edit proposed to this line.
- risk: low

### [UNVERIFIED] Panel geometry must be fractional — the cinematic viewport is browser-aspect and recomputed on every resize
- where: game.js:6096
- now: _setCinematicViewport (line 6092) sets `CINEMA_VW=Math.max(640,Math.round(VH*ar))` where ar is window.innerWidth/innerHeight clamped at .85, then resizes the backing store. setState calls it for GS.CUTSCENE / GS.CAMPAIGNINTRO / GS.VICTORY / GS.STAGECLEAR (line 25540 region) and window.__bofResizeCinematic re-runs it on every resize (line 6109-6113). So W is not a constant: it is 640 at minimum and unbounded on an ultrawide monitor, while H is always exactly VH=512.
- FIX: Express every gutter and panel edge as a fraction of W (or as a px value derived from H, which is fixed) inside cinPanelFrame — never a literal px of width. Note the existing aperture at 40559 already gets this right by using H*.052. No edit to this line.
- risk: low

### [UNVERIFIED] Whole-frame black wipes would paint straight over any panel border
- where: game.js:40568
- now: Beat transitions are full-frame fills, drawn AFTER the aperture bars: `const fadeIn=clamp(1-hqBeatT/.24,0,1),fadeOut=clamp((hqBeatT-(dur-.34))/.34,0,1)` then a `ctx.fillRect(0,0,W,H)` at 40569. The campaign intro does the same at line 40539 with a .42s fade, and its beat crossfade at 40533-40536 redraws the next backdrop full-frame at rising alpha. Because these cover 0,0,W,H, a border painted before them vanishes during every transition and reappears after — the frame would appear to flicker on and off at each cut.
- FIX: Sequence, not geometry: call cinPanelFrame AFTER the fade fills rather than before, i.e. move the frame paint to the end of drawCutsceneBeats (below line 40570) and of drawCampaignIntro (below 40539). The border then holds steady while the picture inside it fades, which is also the correct comic read — the panel does not blink, the art in it changes.
- risk: low

## Cinematic motion renderers — audit of cinDrawMotion and every motion it handles, against Mike's two required modes: (a) top-down vertically-scrolling gameplay-style shootdown, (b) two top-down jets side by side cutting to dialogue windows with face avatars.

### [UNVERIFIED] VERDICT: which motions match (a) or (b) — the camera table
- where: game.js:40240
- now: cinDrawMotion dispatches 13 live motions plus a dead one. The cinematic ship pack registers seven views per pilot (line 1984: 01_top_down, 02/03_front_3q, 04/05_rear_3q, 06_hard_bank, 07_center_rear). Which view each motion actually asks for: formation/cruise/radar/sos -> views 2 and 3, front three-quarter (line 40238). aftermath -> same, plus two airbursts (40243). dogfight -> view 6 hard bank (40248). barrel -> cycles [2,6,3,1] (40248). duo_attack -> views 2 and 3 (40258). missile_lock -> view 6 (40266). joyride -> view 2 hero, view 7 traffic (40275-40276). cloak -> views 2 and 3 (40295). hq_approach / jungle_approach / depart -> view 7 center-rear (40302, 40310, 40318). ram -> view 1, TOP DOWN (40279). So: NOTHING matches mode (a) fully. NOTHING matches mode (b). Exactly one motion — ram — uses the top-down view at all, and it is also the only one whose hero moves vertically down the frame (`push` drives y at 40279), which makes it the closest existing thing to (a). Every other motion is three-quarter or chase-cam.
- FIX: No code change in this finding — this is the inventory the rest of the findings act on. The two closest starting points are cinFormation (the (b) candidate, one view number away) and the ram branch of cinDrawMotion (the (a) candidate, needing a scrolling backdrop and top-down enemy art).
- risk: none — read-only inventory

### [UNVERIFIED] Mode (b): cinFormation is one literal away from two top-down jets side by side
- where: game.js:40238
- now: The two-ship draw is `cinDrawShip(s2.p, s2.left?2:3, ...)` — views 2 and 3 are 02_front_left_3q and 03_front_right_3q, so the pair is always seen head-on in three-quarter, never overhead. Everything else Mike asks for in mode (b) is already here: two pilots abreast, the speaker forward and brighter (alpha 1 vs .78), the listener eased back, correct far-to-near draw order (40236), a per-ship z breathe. This one renderer serves formation, cruise, radar, sos and aftermath — 32 of the 51 beats in PILOT_OPENINGS.
- FIX: In cinFormation, change the view literal on line 40238 from `s2.left?2:3` to `1` (01_top_down). `cinship_<pilot>_1` is registered for all nine pilots at line 1987 and cinShipKey clamps 1..7 (line 40079), so no art or loader work. Keep the x/y/z/alpha maths exactly as-is; only the plate changes. Optionally drop the ±.018 roll on the same line, since a top-down plate reads odd tilted.
- risk: low — one literal; affects every formation/cruise/radar/sos/aftermath beat at once, so it is also the one change to show Mike first

### [UNVERIFIED] beat.view is read in only one branch and no scene ever sets it
- where: game.js:40223
- now: The single-cast branch of cinFormation honours `beat.view||2`. The two-cast branch (line 40238) ignores beat.view entirely and hardcodes 2/3. And `view:` appears zero times in the whole of game.js — no beat has ever set it. This is the file's own recurring 'declared and never fired' shape, the same one the CIN_HOSTILES[2] comment at 40167 records.
- FIX: On line 40238 pass `beat.view||1` instead of a bare literal, mirroring line 40223. That makes the top-down flip in the previous finding the default while letting an individual beat opt back to a three-quarter shot by adding `view:2` to its data — no renderer fork needed.
- risk: low

### [UNVERIFIED] Four named motions collapse into one renderer, so radar and sos show nothing radar-like
- where: game.js:40242
- now: `if(motion==='formation'||motion==='cruise'||motion==='radar'||motion==='sos'){cinFormation(...);return;}` — four motion names, one identical picture. Seven beats say 'radar' and four say 'sos' (e.g. line 39832 "my radar is still working", 39860 "comms are down"), and every one of them draws the plain two-ship formation. Mike's 'they make zero sense' complaint is partly literal here: the dialogue names an instrument the frame never shows.
- FIX: Keep the single cinFormation call, but after it add a small motion-gated overlay inside cinDrawMotion at line 40242 — a sweep arc for 'radar', a pulsing carrier-lost glyph for 'sos'. Do not fork cinFormation; the staging is correct, only the prop is missing.
- risk: low

### [UNVERIFIED] Mode (a): no motion scrolls — the backdrop drifts sideways, never bottom-to-top
- where: game.js:40143
- now: cinBackdrop's space branch places stars at `x=(i*173.17+(t*9*(1+i%3)))%W, y=(i*83.71)%H` — x is a function of time, y is constant. The stars drift HORIZONTALLY. The photographic backdrops go through cinCover (line 40133 and neighbours), which pans horizontally too (`sx=travel*pan`). There is no vertical scroll anywhere in the cinematic renderer, so mode (a)'s 'vertically scrolling like in-game' has no backdrop to run on.
- FIX: Swap the two terms on line 40143 so y carries the time term and x is fixed (`x=(i*173.17)%W, y=(i*83.71+t*speed)%H`) for the space case, and add one new scrolling backdrop kind for the new motion rather than changing cinCover, which the dialogue beats still want static.
- risk: low

### [UNVERIFIED] Mode (a)'s backdrop art already exists, is authored for exactly this, and is wired to nothing
- where: game.js:1994
- now: Line 1994 registers ONE plate out of two nine-plate packs: `cinbg_jungle` from cinematic_level_approaches/. The sibling directory assets/game/cinematic_level_transitions_topdown/ holds nine 1672x941 plates — one per campaign stage plus the stage-9 bonus — whose own README states: camera 'true overhead, near-orthographic, 85-90 degrees', 'Scene flow: bottom entry to top exit', 'no baked ships, aircraft, vehicles, pilots, enemies', 'Intended use: place all moving actors and interactive objects over these backgrounds at runtime'. Its manifest.json carries per-stage mappings and sha256s. Grep across assets/game.js, assets/manifest.js and index.html: zero references. The other eight cinematic_level_approaches plates are equally unregistered. This is a finished, verified art drop built for Mike's mode (a) that the game cannot reach — the same shape as the 'four of the seven HQ interiors were registered nowhere' note at line 2009.
- FIX: Add nine `X._src['cintop_stage<NN>']=...` registrations directly beneath line 1994, keyed by stage number, and have the new mode-(a) motion pick its plate from the stage just cleared. 1672x941 is 1.777:1 and the cutscene viewport is CINEMA_VW x VH ~= 911x512 (lines 6091/6096), so these plates cover the cinematic frame with no crop — unlike the portrait gameplay viewport.
- risk: low — pure registration; nothing currently reads these keys so nothing can regress

### [UNVERIFIED] The vertical-scroll sampler mode (a) needs already exists and takes an explicit scroll value
- where: game.js:54082
- now: stage6TransitionBackgroundDraw(scroll) at 54082 is a self-contained bottom-to-top looping sampler: it takes scroll as an argument, wraps against the plate height, and delegates to bg6LoopDraw (line 54682) which does the seamless two-slice wrap. drawLaunch already drives it from one accumulator (drawLaunch._bgScroll). Nothing in the cinematic renderer calls either. Caveat: bg6LoopDraw reaches for `_levelCfg()` for its optional neon-city overlay, which depends on `run`; that block is already inside a try/catch, but a cutscene playing outside a run should not rely on it.
- FIX: Do not reuse bg6LoopDraw directly from the cutscene. Copy its four-line wrap maths (compute sy from scroll modulo natural height, drawImage twice) into the new motion branch in cinDrawMotion so the cutscene owns its own sampler with no `run` dependency. That is ~5 lines, not a new subsystem.
- risk: low

### [UNVERIFIED] Mode (a)'s one real art blocker: every cutscene hostile is three-quarter, deliberately
- where: game.js:40073
- now: CIN_HOSTILES is three keys (line 40073) registered at lines 2043-2045 to cinematic_ships/hostiles/. The comment above them (lines 2030-2045) records that these were regenerated as three-quarter views ON PURPOSE, because the keys previously borrowed live gameplay sprites from stages 5 and 8 which are strict top-down, and that mismatched the three-quarter cinship_* heroes. That decision is correct for the current motions and is exactly backwards for mode (a): a top-down shootdown needs top-down enemies, and the only top-down enemy art in the tree is the live stage-5/stage-8 reels the same comment says must not be touched ('those files are live enemy reels - 9, 16 and 8 sibling frames'). The comment at 40179 already flags generating cutscene enemy art as Mike's call because it spends his SpriteCook credits.
- FIX: No code change until Mike decides. Two options to put to him: (1) add three top-down hostile keys alongside the existing three (cinhostile_scout_td etc.) generated to the same contract, leaving the 3q set in place for the mode-(b) beats; or (2) point the mode-(a) motion at the existing live top-down enemy sprite keys read-only, accepting frame-reel sizing. This is also the blocker on his 'snapshots of the enemies we destroyed'.
- risk: none — flagging a decision, not proposing a change

### [UNVERIFIED] Nothing renders a post-level cutscene, which is the root of 'they don't lead us up to why we're going to space'
- where: game.js:40026
- now: Every motion beat in the game lives in PILOT_OPENINGS (line 39820) and all of it plays once, before stage 1. HQ_AT's post entries (line 40027) route stages 1/3/4/6/7/9 to HQ_ALL_* ids, and those are static-room portrait scenes handled by drawCutsceneEnsemble, not by cinDrawMotion. The PILOT_OPENINGS header says it outright at line 39818: 'post-stage calls now pass straight through'. So there is no renderer anywhere that shows what happened on the level just cleared, and no motion beat between stage 5 and the space stages. Mike's 'talk about what occurred on the previous level' has nowhere to be authored.
- FIX: This is the structural gap, and it is data plus routing, not new rendering: once the two motions below exist, add post-stage entries to HQ_AT (line 40026-40027) pointing at new PILOT_OPENINGS-shaped scenes, and let hqTrigger's existing 'post' path play them through drawCutsceneBeats. hqTrigger already accepts a 'post' when — line 40396 reads HQ_AT[when][stage] generically.
- risk: medium — touching HQ_AT changes campaign flow; the stage-1 pre ordering comment at 40397 documents why order matters there

### [UNVERIFIED] MINIMAL MOTION SET: two new motions, four retired, seven re-pointed
- where: game.js:40242
- now: Thirteen live motions currently cover three cameras (three-quarter, chase, one top-down) and none of Mike's two modes cleanly.
- FIX: Add exactly TWO new branches to cinDrawMotion and re-point the rest. MODE B = 'wingpair': cinFormation with the view literal at 40238 set to 1 — absorbs formation, cruise, radar, sos, aftermath, cloak and joyride (all seven are 'two jets and a conversation'), with radar/sos/aftermath/cloak keeping only their existing overlay differences. MODE A = 'strafe': new branch that draws the scrolling top-down plate (findings above), the hero(es) at view 1 in the lower third moving on the x axis, hostiles entering from the top edge and closing via the existing cinClose (line 40195), and cinFx airbursts on kill — absorbs dogfight, barrel, duo_attack, missile_lock and ram, which are all 'pilots shooting enemies down' and differ today only in ship count and hostile placement. That is 12 of 13 motions expressed as 2. KEEP hq_approach / jungle_approach as-is (view 7 arrival shots are neither mode and Mike did not object to them). DELETE the dead 'depart' branch. Net: cinDrawMotion goes from 13 branches to 4.
- risk: medium — this is the shape of the work, not a single edit; each absorbed motion needs its beat data checked for ship count before its branch is removed

### [UNVERIFIED] 'depart' is a dead motion handler
- where: game.js:40314
- now: cinDrawMotion has a full depart branch (40314-40320) with traffic fly-off, and drawCutsceneBeats special-cases it for backdrop selection (line 40556). Grep for `'depart'` returns those two lines only — no beat in any scene sets motion:'depart'. It has never rendered a frame.
- FIX: Delete the branch at 40314-40320 and the `beat.motion==='depart'?hqSc.destination:` clause on line 40556 when the motion set is consolidated. Its traffic-drawing line 40320 is worth lifting into the new 'strafe' motion first — it is the only code that stages background aircraft at depth.
- risk: low

### [UNVERIFIED] 'do not stretch dialogue boxes' — cinDialogue ignores the plate's aspect, drawCommWindow does not
- where: game.js:40326
- now: cinDialogue sizes its window as a fixed rect: `dw=Math.round(W*.95), dh=Math.round(H*.255)`, then blits the plate into it with drawImage at those dimensions (line 40339). The plate is whatever `dlg_<pilot>` is, and every pilot's is a different shape — so all nine are squashed or stretched to one 0.95W x 0.255H box. The other dialogue renderer in the game does it correctly: drawCommWindow line 51528 computes `ph = pw * (naturalHeight/naturalWidth)` from the plate itself.
- FIX: In cinDialogue, replace the fixed `dh` on line 40326 with the aspect-derived height, copying line 51528's form: derive dh from the chosen key's naturalHeight/naturalWidth, then anchor dy to the bottom margin instead of a fixed .715. Note the key is chosen at line 40337-40338, below the sizing — the two lines need to swap order.
- risk: low — but the body-text box at line 40344 is laid out in fractions of dh, so those fractions need re-checking once dh varies per pilot

### [UNVERIFIED] 'dialogue windows ... with the face avatars in there' — cinDialogue draws no avatar at all
- where: game.js:40324
- now: cinDialogue (40324-40352) draws the frame, the speaker's name, the typed body and a NEXT prompt. There is no portrait. drawCommWindow, the other dialogue renderer, has a full framed portrait inset (lines 51565-51603) driven by o.portraitKey, and pilotPortrait(pilotKey, emo) at line 55018 already resolves per-emotion faces including an animated 'talk' mouth cycle. cinDialogue has beat.who in hand at line 40337 — everything needed is present and unused.
- FIX: In cinDialogue, after the frame blit, call pilotPortrait(beat.who,'talk') and draw it into the left ~36% of the interior, reusing the inset geometry from drawCommWindow lines 51566-51574. Then shift the name/body x origin right by that width. Do not call drawCommWindow itself — it paints a full-screen 0.66 scrim (line 51518) that would black out the motion behind the cutscene.
- risk: low

### [UNVERIFIED] 'slow down transitions so we can read texts' — dialogue beats have no transition at all
- where: game.js:40567
- now: drawCutsceneBeats gates the whole fade block on `if(!text&&dur>0)` (line 40567): only text-less beats get the 0.24s fade-in / 0.34s fade-out at line 40568. Dialogue beats hard-cut from one to the next with no fade and no minimum dwell — they advance the instant the player taps (line 40572), and the typing runs at HQ_CPS=42 chars/sec (line 40051). The longest line in the game (line 39824, Freezer's, ~200 chars) is therefore skippable in one frame.
- FIX: Two surgical changes: (1) on line 40567 drop the `!text` condition so text beats fade too, using a short fixed fade rather than beat.duration; (2) add a minimum-dwell guard to the tap handler at line 40572 so a tap while `hqChars<n` only completes the line (it already does this) and a second tap is refused until ~0.4s after completion. Lower HQ_CPS at line 40051 only if Mike still finds it fast after that — the skip, not the speed, is what makes lines unreadable.
- risk: low

### [UNVERIFIED] 'clean up all frames' — cinFx explosions loop forever instead of playing once
- where: game.js:40199
- now: cinFx picks its frame as `Math.abs(Math.floor(t*13))%8` (line 40200) — an unbounded modulo of the beat clock. The 8-frame airburst therefore re-detonates every 0.6s for as long as the beat runs, and the laser and missile reels do the same. Nothing ever completes. Callers work around it with ad-hoc gates (`if((t%1.6)>.8)` at line 40255, `if(_d4.e>0.55)` at 40288) rather than the effect owning its own lifetime.
- FIX: Give cinFx an optional start time: change the frame index on line 40200 to clamp rather than wrap — `const f=Math.min(7,Math.floor((t-(start||0))*13))` and return early when t is before start or past the last frame. Existing calls pass no start and keep frame 0..7 then stop, which is already better than looping; the caller-side `t%` gates at 40255 and 40263 can then be replaced with a real trigger time.
- risk: low

## Cutscene portrait / face-avatar system (pilotPortrait, port_cf_*, msgFace*, dialogue renderers)

### [UNVERIFIED] Which portrait keys exist per pilot — the approved CF pack
- where: game.js:1970
- now: _CF_PORTRAIT_PILOTS (line 1970) lists all nine pilots: axel, cole, decker, falva, freezer, juggernaut, lizzie, maverick, yuri. _CF_PORTRAIT_POSES (1971-1972) lists TWELVE poses: idle, happy, laugh, anger, sad, crash, victory, talk-closed, talk-small, talk-medium, talk-wide, talk-o. Line 1975 registers X._src['port_cf_<pilot>_<pose>'] = 'assets/game/pilot_portraits/<pilot>-<pose>.png' — 108 keys, all code-owned. The comment at 1966-1969 explains why they are NOT in manifest.js: manifest is generated and has previously reverted hand-edited portrait mappings back to the old atlas. Line 2581 warms only the nine *_idle at boot; every expression and mouth pose stays lazy.
- FIX: Nothing to add — the avatar art Mike wants in the comic dialogue windows already exists at full coverage. The one gap is warming: a cutscene beat that opens on a non-idle emotion will miss for a frame or two. Add the scene's requested poses to cinWarm (assets/game.js:40079), which already gates on dlg_window and the ship views, rather than widening the boot preload at 2581.

### [UNVERIFIED] On-disk verification of the portrait pack — 108 files, 256x256 square
- where: game.js:1975
- now: Directory assets/game/pilot_portraits/ holds exactly 108 PNGs — 12 per pilot for all nine, matching the registration at 1975 with no missing combination and no extras. Sampled dimensions (cole-idle, cole-talk-wide, yuri-anger, juggernaut-idle) are all 256x256, ~83-92KB each. Note the filename uses `happy`, never `smile`.
- FIX: Design the comic avatar bay as a SQUARE, not the tall bust the legacy atlas implies. The existing bays assume portrait-orientation art: dlgBox clamps to pw*0.30 wide by ph*0.74 tall (assets/game.js:32452-32456) and drawCommWindow uses inW*0.36 by full inH (51551). Both will letterbox or crop a 256x256 source.

### [UNVERIFIED] How a portrait is selected — the single resolver, and the talk-mouth cycle
- where: game.js:55003
- now: pilotPortrait(pilotKey, emo) lowercases both args; translates 'smile' to 'happy' (55007) because the CF pack renamed it; and if asked for 'talk' (55008-55012) cycles a seven-entry mouth array ['talk-closed','talk-small','talk-medium','talk-wide','talk-o','talk-medium','talk-small'] on a wall clock at 85ms per frame. Resolution order is port_cf_<p>_<e> (55014) -> port_cf_<p>_idle (55016) -> legacy port_<p>_<e> with talk-* collapsed back to idle (55018-55020) -> port_<p>_idle (55022) -> face_<p> (55023). Every fallback is gated on XART.rdy, so a still-decoding frame degrades instead of drawing blank.
- FIX: Reuse as-is — it is the one resolver every surface should call, and it already handles both the held-expression and the lip-flap cases. For comic panels, pass 'talk' only while characters are still arriving and the beat's authored emotion once the line is complete; that is exactly the ternary dlgBox uses at 32433-32434.

### [UNVERIFIED] THE ANSWER: the beats cutscene dialogue renderer draws NO portrait at all
- where: game.js:40309
- now: cinDialogue(beat,budget,W,H) is the dialogue renderer for the per-pilot campaign openings (called from drawCutsceneBeats at 40549). It draws four things and nothing else: the speaker's frame plate at 40323-40324, the speaker NAME via msgTextLeft at 40327, the body block via msgDrawBlock at 40329-40331, and a blinking NEXT at 40332-40334. There is no pilotPortrait call, no XART.get of any port_ key, and no bay reserved — padX is dw*.055 and the body column is the full dw-padX*2. Mike's nine pilots speak their entire opening with no face on screen.
- FIX: In cinDialogue, add the bay after line 40324: resolve `const _pk=String(beat.who||'').toLowerCase()` and `const _portK=pilotPortrait(_pk, budget<beat.text.length?'talk':(beat.emo||'idle'))`, draw it into a square box at dx+padX of side ~dh*0.8 (source is 256x256), then set `const bay=side+padX` and add `bay` to the x passed to msgTextLeft at 40327 and to msgDrawBlock at 40329 while subtracting it from that block's `w`. Do not draw a second border — the dlg_ plate already carries one.
- risk: The bay MUST be subtracted from the body width, not just added to its x. cinDialogue solves its wrap through msgDrawBlock in the 2x `cutscene` BMF face (msgFaceBig at 40310); shifting x without narrowing w runs the text out over the right rail — the exact fault recorded at 3860-3868 and again at 4069-4079.

### [UNVERIFIED] The ensemble HQ cutscene renderer draws bodies and an emblem, never a face
- where: game.js:4050
- now: drawCutscene (3894-4093) is the other cutscene renderer, used by drawCutsceneEnsemble for the HQ_SCENES briefings. It stages FULL-BODY art: cutPose (3845-3854) returns cinseat_<pilot> for the seated ensemble row or pose_<pilot>_<n>, composited at 3964-4010. Inside the dialogue panel it draws the pilot EMBLEM pemb_<pilot> beside the name (4051-4062, keys confirmed in manifest.js for all nine) and the body text (4084-4088). pilotPortrait is never called in this function; the word 'portrait' throughout it refers to the body cutouts, not faces.
- FIX: For Mike's comic dialogue windows, swap the emblem for the avatar at 4058-4062: replace `XART.get(ek)` where ek='pemb_'+spk.pilot with `XART.get(pilotPortrait(spk.pilot, sc.speakingEmo||'idle'))`, drawn as a square. Leave the ensemble body compositor at 3964-4010 untouched — it is a separate concern and Mike's brief keeps top-down jets for the ship shots, not the room.
- risk: The emblem and the name are centred together as one unit — _nx at 4061 solves `dx+dw*.5-(ew+S(6)+_nameW)*.5`. A square 256x256 avatar at S(22) height is far wider than the emblem, so the centring arithmetic needs re-solving, not just a key swap. If it is not, the name slides off-centre.

### [UNVERIFIED] Ensemble scene lines carry a BODY pose index, not an emotion
- where: game.js:39905
- now: Every HQ_SCENES line is a three-element array [pilotKey, poseIndex, text] — e.g. ['cole',0,"Every military network on Earth is compromised..."] at 39905. The middle number is consumed by cutPose as 'pose_'+pilotKey+'_'+pose (3850), i.e. a full-body pose, NOT a face emotion. hqSeat (40049-40056) copies it into hqSlot[side].pose and drawCutsceneEnsemble forwards it as sc.speakingPose (40580, 40583). There is no emotion field anywhere in the eight authored scenes.
- FIX: Add an optional fourth element ln[3] for the face. hqSeat already destructures ln[0]/ln[1] at 40051 — extend it to carry `emo: ln[3]||'idle'` into hqSlot, and have drawCutsceneEnsemble set `speakingEmo: ln[3]||'idle'` on both branches of the sc it builds at 40577-40584. That is the field finding 5's avatar draw reads. Existing three-element lines keep working with no migration.

### [UNVERIFIED] Opening beats carry no emotion field either
- where: game.js:39808
- now: PILOT_OPENINGS beats (39805 onward) come in two shapes: action beats {motion, bg, duration} and dialogue beats {who, motion, text} — e.g. {who:'axel',motion:'formation',text:"You okay over there, bud? What the hell was that back there?!"} at 39808. `who` is a pilot key resolved through PILOTS by cinPilotName (40060-40063). No emo, emotion, face or portrait field exists on any beat in any of the nine openings.
- FIX: Add an optional `emo:` to the dialogue beats worth making expressive — vocabulary is the twelve poses listed at 1971-1972 — and read it in cinDialogue as `beat.emo||'idle'` per finding 4. No schema migration: a beat with no emo falls to idle.

### [UNVERIFIED] The one renderer that DOES draw a face — copy this block
- where: game.js:32423
- now: dlgBox, the in-play (non-modal) dialogue window, is the only place in the file where the CF avatar system is fully wired. 32426-32429 derives the pilot key from o.portrait||who and gates it on PILOTS plus a hardcoded nine-name list; 32432 sets _talking = shown.length<full.length; 32433-32434 calls pilotPortrait(_pk, _talking?'talk':(o.emo||'idle')) so mouths animate while typing and settle to the authored emotion when the line completes; 32450-32461 clips a bay at x+12 of width min(bw, pw*0.30) and height ph*0.74, MIRRORS it via translate(bx+bw,by)+scale(-1,1) at 32457, and sets `bay` so both the name (32464-32468) and the body (32477-32480) shift right by it. The panel also widens from 300 to 344 when a portrait is present (32438).
- FIX: Lift this block into cinDialogue essentially verbatim — same resolver call, same talk/settle ternary, same 'bay shifts the whole text column' arithmetic. Change only the box shape: drop the pw*0.30 width clamp in favour of a square, since the CF source is 256x256.
- risk: The mirror at 32457 is load-bearing and is recorded twice (comment at 32423-32425, and CLAUDE.md:1643 'Cutscene portraits face each other'). Every portrait in the pack is authored facing SCREEN-LEFT, so an unmirrored avatar in a left-hand bay faces away from its own speech balloon.

### [UNVERIFIED] drawCommWindow's portrait bay, and the only renderer that respects the plate's aspect
- where: game.js:51550
- now: The modal comm panel takes a caller-supplied o.portraitKey with o.cardKey as backup (51552), COVER-fits it into a bay of inW*0.36 by inH (51551, 51559-51568) with a recessed dark plate behind and a pilot-tinted border in front (51571-51587). Callers are drawPilotComm (51638-51639, 'idle'), the rival dialogue (55487) and the two rival spare/spared paths (55607, 55628) — all four hardcode 'idle'. Separately and importantly, line 51513 derives the panel HEIGHT from the art's own aspect: `Math.round(pw*(XART.get(_fk).naturalHeight/XART.get(_fk).naturalWidth))` whenever a dedicated dlg_<pilot> frame is up.
- FIX: Two uses. The bay at 51550-51588 is a second reference implementation for the comic avatar (framed, cover-fit). Line 51513 is the pattern to copy for Mike's 'do not stretch dialogue boxes' — it is the ONLY dialogue surface in the file that already sizes its panel from the plate instead of from screen fractions.

### [UNVERIFIED] Rival dialogue throws away emotion tags that are already authored
- where: game.js:55484
- now: RIVAL_SCRIPT (55029-55054) tags rival lines with emotions: maverick anger then smile, yuri idle then anger, decker idle then smile, juggernaut anger then laugh. rival.script is set from that table at 55370. But the renderer at 55484 reads `const _emo='idle';   // no emotion 'emoji' portrait swaps — always the idle face` and passes that constant to pilotPortrait at 55487. `line.emo` is never read anywhere. The spare and player-spared panels at 55607 and 55628 likewise hardcode 'idle'.
- FIX: One-line change if Mike wants these faces expressive: at 55484 use `const _emo=(line && line.emo) || 'idle';`. The data and the art (anger/happy/laugh for all nine) already exist. Ask first — the inline comment says the flat idle face was a deliberate past call, so this is a reversal, not a bug fix.
- risk: Reverting a decision the comment records as intentional. Also note docs/PASSOVER.md:287 describes those emo tags as WIRED ('rival script lines carry emo tags'), which is stale against the code at 55484 — do not trust the doc over the line.

### [UNVERIFIED] The legacy fallback keys behind the CF pack
- where: game.js:55019
- now: Below the CF pack pilotPortrait falls to the old atlas. Confirmed in assets/manifest.js: port_<pilot>_{idle,smile,anger,laugh,sad,crash,victory} exists for all nine pilots — SEVEN emotions and it is spelled `smile`, not `happy`, which is why 55007 and 55018 translate in both directions. Also present for all nine: face_<pilot> (the idle alias) and card_<pilot> (the full 820x631 roster sheet, per the note at 51967-51978 — explicitly NOT an avatar). All live in assets/game/atlas/ui_dialogue.png; the cells are non-square and vary per pilot (port_cole_idle is 249x293, port_lizzie_idle 295x399, port_axel_idle 175x265).
- FIX: Leave the chain alone — it is the decode-window safety net. But size the new comic bay to the CF pack's 256x256 and treat the legacy cells as degrade-only; a square bay fed a 295x399 legacy cell will crop or letterbox, and card_<pilot> in an avatar slot gives you a tiny pilot floating in a stat sheet (the failure the comment at 51967-51972 already describes).

### [UNVERIFIED] 'Do not stretch dialogue boxes' — cinDialogue stretches the plate ~3.7x horizontally
- where: game.js:40311
- now: cinDialogue hardcodes dw=Math.round(W*.95) and dh=Math.round(H*.255), then drawImage's the frame into that box at 40324 with no aspect derivation. W is cutsceneViewWidth() = CINEMA_VW, computed at 6096 as max(640, VH*window aspect); H is VH=512. On a 16:9 window that is roughly 865x131, aspect ~6.6:1. The plates being poured into it are dlg_window at 1465x808 (manifest cell ['ui_dialogue',0,0,1465,808], aspect 1.81) or a dlg_<pilot> plate at 569x321 (aspect 1.77). That is a 3.7x horizontal stretch of authored art — Mike's complaint, with a number on it.
- FIX: Solve the box from the plate instead of from screen fractions, the way drawCommWindow already does at 51513: `const im=XART.get(_dk); const dh=Math.round(dw*im.naturalHeight/im.naturalWidth);` and derive dy as H-dh-margin. At dw=865 true aspect gives dh=477, which will not fit VH 512 — so the honest fix is a narrower panel (comic panels are not full-bleed anyway): choose dw so dh lands near H*0.30, i.e. dw about 280.
- risk: Every offset in the function is a fraction of dw/dh (padX, nameY, bodyY, bodyH, and the NEXT prompt at 40332-40334), so they follow automatically — but the body will re-wrap into a much narrower column and maxH at 40330 may need lowering. Verify against the longest authored line (freezer's second beat at 39809 is the worst case in PILOT_OPENINGS).

### [UNVERIFIED] The ensemble panel stretches dlg_window identically
- where: game.js:4012
- now: drawCutscene uses the same hardcoded box — dx=CW*.025, dy=CH*.715, dw=CW*.95, dh=CH*.255 (4012) — and drawImage's dlg_window into it at 4019 with no aspect derivation, giving the same ~6.6:1 stretch of a 1.81 plate. This compounds a second problem: the interior insets DLGW_IN_X/W/Y/H at 3879 were MEASURED off the plate's own proportions (the derivation is written out at 3856-3877), so once the box is scaled non-uniformly those fractions no longer land on the frame's real rails.
- FIX: Same one-line fix as finding 12, applied at 4012: derive dh from XART.get('dlg_window').naturalHeight/naturalWidth rather than CH*.255, and narrow dw so the result fits. Fixing this is what makes the measured DLGW_IN_* constants correct again rather than approximately correct.

### [UNVERIFIED] 'Slow down transitions so we can read texts' — the two constants that control it
- where: game.js:40036
- now: HQ_CPS=42 characters per second drives both cutscene renderers (40547 for beats, 40611 for ensemble). Action beats hold for beat.duration with a 0.24s fade-in / 0.34s fade-out (40552-40556). Dialogue beats have NO minimum dwell at all: the input check at 40557 fires from hqBeatT>0.18, so the first tap completes the line and the second advances it — a fast tapper clears an entire beat in about a fifth of a second, and there is no cross-fade between consecutive dialogue beats.
- FIX: Two surgical knobs. (1) Lower HQ_CPS at 40036 to about 28-32. (2) In drawCutsceneBeats at 40557, gate the advance on a dwell after the line completes rather than on hqBeatT>0.18 alone — record when hqChars first reaches n and require ~0.6s past it before the second tap advances. Do not restructure the loop; both are local.
- risk: HQ_CPS is shared by drawCutsceneEnsemble (40611), so lowering it slows the HQ briefings as well as the openings. That is probably what Mike wants, but it is one constant serving two surfaces — if he wants them to differ, they need splitting first.

### [UNVERIFIED] A stale ⚠ comment says cinDialogue is dead code; it is the live renderer
- where: game.js:39899
- now: The header above HQ_SCENES states: '⚠ cinDialogue is now referenced by nothing - it was the beats renderer for the openers. Left on disk rather than deleted'. That is false. drawCutsceneBeats calls cinDialogue(beat,hqChars,W,H) at 40549; drawCutsceneState routes to drawCutsceneBeats whenever hqMode==='beats' (40532); and hqPlayPilot sets hqMode='beats' at 40342. The same paragraph already corrects an earlier stale claim two lines above it (39894-39898, which records that believing the previous stale note 'cost a session's start').
- FIX: Delete lines 39899-39901 as part of the cinematic work. Leaving it will cost the next session the same detour the paragraph directly above it documents.

### [UNVERIFIED] Existing precedent for a rank/state to emotion table
- where: game.js:56223
- now: The stage-clear screen already solves 'pick an emotion for a situation': SC_FACE={S:'victory',A:'victory',B:'laugh',C:'idle',D:'sad',F:'crash'} at 56223, and scPortrait(pilot,rank) at 56224-56230 routes the choice through pilotPortrait then degrades to port_<pilot>_idle / port_<pilot> / card_<pilot>. rivalPortrait (55025-55028) does the same for 'victory'/'crash'/idle.
- FIX: Model any new cutscene emotion table on SC_FACE — same shape, same pilotPortrait delegation, same degrade chain — rather than inventing a parallel mapping. Note CLAUDE.md:1557 records a prior false alarm about SC_FACE.B; the table is correct and was verified by contact sheet.

### [UNVERIFIED] Dialogue frame plates available per pilot (context for the SpriteCook comic windows)
- where: game.js:40323
- now: Nine tinted per-pilot plates exist plus the generic silver one — dlg_axel, dlg_cole, dlg_decker, dlg_falva, dlg_freezer, dlg_juggernaut, dlg_lizzie, dlg_maverick, dlg_yuri (each 569x321 in ui_dialogue.png; dlg_falva is additionally registered loose at 1917) and dlg_window (1465x808). cinDialogue picks the speaker's own plate at 40323 with dlg_window as fallback — the comment at 40312-40321 records that this line asked for dlg_window unconditionally until 0905 and that the bare silver panel was 'the white slab across the bottom of every cutscene', measured at 1.08% of the frame in rows y 736-968.
- FIX: When SpriteCook generates the new comic-style cutscene windows, produce a per-pilot set matching the existing dlg_<pilot> naming and roughly the authored 569x321 (1.77) aspect, plus one generic. Then cinDialogue and drawCutscene need only a key-prefix change, not a layout rewrite. Generating one wide plate for a stretched full-bleed bar would re-create the problem findings 12 and 13 describe.
- risk: If the new plates ship at a different aspect than 1.77 and the hardcoded CH*.255 boxes are left in place, the new art is stretched exactly as the current art is — the frame swap must land together with the aspect fix, not before it.

## Cutscene / cinematic system — complete map (states, scene data, beat schema, motion vocabulary, top-level draw)

### [UNVERIFIED] State machine — the cinematic states and their draw dispatch
- where: game.js:6820
- now: GS declares five cinematic-class states on one line: CUTSCENE:'cutscene', CAMPAIGNINTRO:'campaignintro', VICTORY:'victory', plus OPENING/OUTBOUND/FLYOVER/RIVAL/WARPENTRY/STAGECLEAR. The frame dispatch is a switch: line 39477 `case GS.CUTSCENE: return drawCutsceneState(dt)`, line 39496 `case GS.VICTORY: return drawVictory(dt)`, line 39502 `case GS.CAMPAIGNINTRO: return drawCampaignIntro(dt)`. setState at line 25540 calls `_setCinematicViewport(s===GS.CUTSCENE || s===GS.CAMPAIGNINTRO || s===GS.VICTORY || s===GS.STAGECLEAR)`, which widens the backing store to browser aspect (CINEMA_VW = max(640, round(VH*ar)), game.js:6092-6106) and adds body class 'cinematic-full' (index.html:86-97).
- FIX: No change needed to the state list itself. Anything added for the comic treatment must hang off these four states only; note that OPENING/OUTBOUND/FLYOVER/RIVAL are NOT in the cinematic viewport list, so they stay 480 wide and must not share a comic-panel layout built against CINEMA_VW.
- risk: none — inventory only

### [UNVERIFIED] GS.CUTSCENE is two renderers behind one state, chosen by hqMode
- where: game.js:40546
- now: `function drawCutsceneState(dt){ if(hqMode==='beats') return drawCutsceneBeats(dt); return drawCutsceneEnsemble(dt); }`. hqMode is a module global declared at game.js:40048 (`let hqSc=null, hqLine=0, hqChars=0, hqDone=null, hqSeen={}, hqMode='ens', hqBeatT=0;`) and is set to 'beats' only by hqPlayPilot (40354) and to 'ens' only by hqPlay (40368). The two renderers read DIFFERENT fields of hqSc: drawCutsceneBeats reads `hqSc.beats[hqLine]`, drawCutsceneEnsemble reads `hqSc.lines[hqLine]`. A scene entered without setting hqMode draws one data shape through the other's loop and produces a blank screen, which the comment at 40040-40046 records.
- FIX: Leave the switch. Any new comic renderer should be a THIRD hqMode value ('comic') with its own entry point that sets hqMode, rather than being grafted into drawCutsceneBeats — the file's own note says a mode that can be entered without being set is the blank-screen fault.

### [UNVERIFIED] Scene data structure #1 — PILOT_OPENINGS (the beats scenes, nine of them)
- where: game.js:39820
- now: `const PILOT_OPENINGS={ axel:{...}, freezer:{...}, cole:{...}, falva:{...}, maverick:{...}, yuri:{...}, juggernaut:{...}, lizzie:{...}, decker:{...} }` — one scene per pilot, ending at game.js:39889. SCENE-level fields, all read by the renderer: `cast` (array of pilot keys; cast[0] is the lead, only cast.slice(0,2) is ever drawn — 40224, 40300, 40316); `traffic` (array of pilot keys drawn as small background aircraft, only in motions 'joyride' 40276 and 'depart' 40320; only yuri declares it); `destination` ('hq' or 'jungle' — used as the bg fallback for a 'depart' beat at 40556 and to pick the warm-list background at 40086); `beats` (the ordered array). There is NO id, NO title on these scenes.
- FIX: For a comic presentation, add scene-level fields here rather than per-beat: a `panels` layout hint and a `caption` (yellow narration box) per beat. Do not rename cast/traffic/destination/beats — cinWarm (40094), cinAllPilots (40080) and cinBackgroundKeys (40084) all key off those exact names.

### [UNVERIFIED] Scene data structure #2 — HQ_SCENES (the ensemble scenes, eight of them) and its LINE schema
- where: game.js:39918
- now: `const HQ_SCENES = [ {id:'HQ_ALL_00', title:"Nine Chairs, Eight Names", lines:[ ['cole',0,"Every military network on Earth is compromised..."], ... ]}, ... ]` — eight scenes HQ_ALL_00..HQ_ALL_07, ending 39984. A scene has exactly three fields: `id`, `title` (never drawn anywhere — grep shows no reader), `lines`. A LINE is a fixed 3-tuple positional array, NOT an object: `[whoKey, poseIndex, text]`. hqSeat (40064) reads ln[0] and ln[1]; drawCutsceneEnsemble (40580) reads ln[2] as `full`. There is no per-line motion, duration, bg or camera — an ensemble scene is a static room plate plus typed text.
- FIX: If ensemble scenes get comic panels, convert `lines` entries to objects ({who,pose,text,panel,caption}) in ONE pass and update the three readers: hqSeat (40064-40070), drawCutsceneEnsemble's `const ln=hqSc.lines[hqLine]` / `const full=ln[2]` (40580-40582), and the sc literal it builds at 40597-40604. A positional tuple cannot carry the extra comic fields.
- risk: medium — three readers, all in one 40-line window; a missed one silently draws the wrong slot

### [UNVERIFIED] COMPLETE BEAT SCHEMA — every field a beat can carry and who reads it
- where: game.js:40551
- now: A beat is a plain object with SIX possible fields; nothing else is read anywhere. (1) `who` — pilot key, optional; read by cinFormation as the active speaker (40220) and by cinDialogue for the name, the name colour and the per-pilot frame (40337, 40341). (2) `text` — string, optional; its PRESENCE is the beat's whole mode switch: with text the beat waits for a tap and gets 70% of the screen height for art (40557), without text it is timed and gets 94% plus letterbox bars (40559). (3) `motion` — string, defaults 'formation' (40241). (4) `bg` — backdrop kind string, optional, with a fallback chain at 40556. (5) `duration` — seconds; ONLY auto-advances a textless beat (40566-40571), but is also read as the easing window by cinFormation/cinClose on every beat (40221, 40252, 40260, 40268, 40281, 40299, 40307, 40315). (6) `view` — integer 1..7, read in exactly one place (40223). Fields are read directly off the literal; there is no defaults/normalise step.
- FIX: Add comic fields as new optional keys read only by the new draw path (e.g. `caption` for a yellow narration box, `balloon` for tail direction, `hold` for a minimum readable time), and give drawCutsceneBeats a single `const B=normaliseBeat(beat)` at its top so defaults live in one place instead of being re-defaulted at eight call sites.

### [UNVERIFIED] COMPLETE MOTION VOCABULARY — every value cinDrawMotion understands
- where: game.js:40240
- now: cinDrawMotion is a flat if-chain, 40241-40322. The renderer understands exactly these 15 strings, in this order: 'formation', 'cruise', 'radar', 'sos' (all four fall into one branch at 40242 and call cinFormation); 'aftermath' (40243, formation plus two airbursts); 'dogfight' (40247); 'barrel' (40247, shares the dogfight branch, cycling views [2,6,3,1]); 'duo_attack' (40257); 'missile_lock' (40265); 'joyride' (40274); 'ram' (40278); 'cloak' (40294); 'hq_approach' (40298); 'jungle_approach' (40306); 'depart' (40314). Anything else falls through to the default `cinFormation(sc,beat,t,W,H)` at 40322. Six of these hard-code a specific pilot rather than reading sc.cast: 'joyride' draws 'yuri' (40275), 'ram' draws 'juggernaut' (40279), 'cloak' draws 'decker' and 'maverick' (40295-40296).
- FIX: Do not extend this chain for the comic pass. Replace the three hard-coded-pilot branches ('joyride' 40275, 'ram' 40279, 'cloak' 40295-40296) with `lead`/`sc.cast[1]`, which is already computed at 40241 — otherwise a new comic scene reusing those motions draws the wrong aircraft. Add new motions ('topdown_pair', 'topdown_scroll') as new branches BEFORE the default at 40322.
- risk: low — the three hard-coded pilots happen to match today's data, so the change is behaviour-neutral for shipped scenes

### [UNVERIFIED] 'cruise', 'radar' and 'sos' are aliases — they render identically to 'formation'
- where: game.js:40242
- now: `if(motion==='formation'||motion==='cruise'||motion==='radar'||motion==='sos'){cinFormation(sc,beat,t,W,H);return;}` — cinFormation (40204) never reads beat.motion except for the unreachable 'barrel' test at 40223, so all four produce the same two banking jets. The authored data leans on them meaning something: freezer's beat at 39832 is `{who:'freezer',motion:'radar',bg:'jungle',text:"Wait. My radar is still working..."}` and yuri/maverick/juggernaut all use `motion:'sos'` for dead-comms lines (39853, 39860, 39868, 39869). Nothing draws a radar sweep or a comms-failure overlay.
- FIX: Give 'radar' and 'sos' their own branches in cinDrawMotion before line 40242 — 'radar' a sweep ring over the formation, 'sos' a dead-comms static overlay. This is the cheapest single change that makes a scene 'make sense': the dialogue already narrates these and the picture currently contradicts it.

### [UNVERIFIED] The `view` field is dead for every shipped beat
- where: game.js:40223
- now: `const p=cast[0],view=beat.motion==='barrel'?[2,6,3,1][Math.floor(t*4)%4]:(beat.view||2);` is the only read of beat.view in the file, and it sits inside cinFormation's `if(cast.length===1)` branch. The two-cast path below it (40238-40239) hard-codes `s2.left?2:3`. No beat in PILOT_OPENINGS declares `view`. Additionally the `beat.motion==='barrel'` test on this line is UNREACHABLE: cinDrawMotion routes 'barrel' into the dogfight branch at 40247 and returns, so cinFormation can only ever be entered with motion in {formation, cruise, radar, sos}, with a synthesised {who:null,motion:'formation'} from 'aftermath' (40244), or via the default fallback at 40322 for an unknown string.
- FIX: In cinFormation, honour `beat.view` on the two-cast path too — replace `s2.left?2:3` at 40239 with `beat.view||(s2.left?2:3)`. That single token is what lets a comic beat request the top-down plate (view 1) for 'their top down jets flying next to each other'. Delete the unreachable `beat.motion==='barrel'?[2,6,3,1][...]:` ternary at 40223 or leave it with a note; do not build on it.
- risk: low — additive, existing beats declare no view so they keep 2/3

### [UNVERIFIED] The top-down cinematic plate exists for all nine pilots and is used in exactly one beat
- where: game.js:1984
- now: `const _CIN_VIEWS=['01_top_down','02_front_left_3q','03_front_right_3q','04_rear_left_3q','05_rear_right_3q','06_hard_bank','07_center_rear'];` registered as cinship_<pilot>_1..7 for all nine pilots (1983-1988). View 1 is the strict top-down, nose-up plate — the same orientation as gameplay. cinDrawMotion asks for view 1 in exactly ONE place, the 'ram' beat at 40279 (`cinDrawShip('juggernaut',1,...)`); campaignIntroFinale uses it once more at 40509. Every dialogue beat uses views 2/3 (front three-quarter) and every travel beat uses view 7 (centre rear).
- FIX: No art generation needed for Mike's top-down request. Add a 'topdown_pair' branch to cinDrawMotion (before the default at 40322) that draws cast[0] and cast[1] at view 1 side by side over a vertically scrolling backdrop, and route the conversational beats in PILOT_OPENINGS to it. The plates are already registered and already warmed by cinWarm (40095, which loops v=1..7).

### [UNVERIFIED] COMPLETE bg VOCABULARY — every backdrop kind cinBackdrop understands, and the fallback chain
- where: game.js:40130
- now: cinBackdrop(kind,t,W,H) understands: 'hq_route' (40131, pans cinbg_hq_aerial 0.28→0.46 over 3.75s), 'hq' and 'hq_aerial' (40132), 'coast' (40133, cinbg_hq_beach), 'hq_gate' (40134), 'jungle' (40135, cinbg_jungle), 'space' (40136 + starfield 40142-40147), 'darksky' (40138-40139 gradient + cincloud_storm). ANY other string, including 'sky', falls to the same day gradient. The beat's bg is resolved at 40556: `beat.bg || (motion==='depart' ? hqSc.destination : (motion==='dogfight'||motion==='missile_lock' ? 'space' : (motion==='cloak' ? 'darksky' : 'sky')))`. Note 'sky' is not a case — it is the default-gradient path by omission.
- FIX: Leave the vocabulary. For 'vertically scrolling' top-down beats, the gap is that the procedural sky path (40137-40156) draws two STATIC cloud blits at fixed positions (W*.05,H*.18 and W*.62-w*.25,H*.06) with no time term — add `(t*speed)%H` wrapping to those two drawImage y values so a top-down beat scrolls like gameplay. cinCover (40118) also has no vertical term.

### [UNVERIFIED] THE DIALOGUE BOX IS STRETCHED ~3.6x HORIZONTALLY — beats renderer
- where: game.js:40326
- now: cinDialogue does `const dx=Math.round(W*.025),dy=Math.round(H*.715),dw=Math.round(W*.95),dh=Math.round(H*.255);` then a plain `ctx.drawImage(XART.get(_dk),dx,dy,dw,dh)` at 40339. The comment at 3865 records the plate's real size: dlg_window is 1465x808, aspect 1.81:1. In the cinematic viewport (CINEMA_VW = round(512*innerAspect); ≈910 on a 16:9 window, VH=512) the destination box is ≈864x131 — aspect 6.6:1. The machined frame, its bevels and its rivets are drawn 3.6x wider than tall. This is Mike's 'do not stretch dialogue boxes' verbatim.
- FIX: In cinDialogue, either (a) clamp width to the plate's aspect — compute `const im=XART.get(_dk); dw=Math.min(Math.round(W*.95), Math.round(dh*im.naturalWidth/im.naturalHeight)); dx=Math.round((W-dw)/2);` — or (b) nine-slice it using the exact pattern already working in this file at 57044-57069 (corners at min(pw/IW,ph/IH), edges stretched on one axis only). (b) is the right one if the box must stay full-bleed wide.
- risk: low — geometry only; the text column at 40340-40347 is derived from dw/dh so it follows automatically

### [UNVERIFIED] THE DIALOGUE BOX IS STRETCHED THE SAME WAY IN THE ENSEMBLE COMPOSITOR
- where: game.js:4012
- now: drawCutscene repeats the identical numbers: `const dx=Math.round(CW*.025), dy=Math.round(CH*.715), dw=Math.round(CW*.95), dh=Math.round(CH*.255);` then `ctx.drawImage(XART.get('dlg_window'), dx, dy, dw, dh)` at 4019. Same 6.6:1 destination for a 1.81:1 plate. It also always fetches the BARE 'dlg_window' — the per-speaker tinted frame lookup that cinDialogue got at 40337-40338 (`const _dk=(_who && XART.rdy('dlg_'+_who)) ? ('dlg_'+_who) : 'dlg_window'`) was never applied here, so the eight ensemble HQ scenes still show the generic silver slab the 0905 note calls 'the white slab across the bottom of every cutscene'.
- FIX: Two surgical edits in drawCutscene: apply the same aspect clamp or nine-slice at 4019, and copy the two-line per-speaker frame lookup from 40337-40338, keying off `spk.pilot` (spk is already resolved at 4022, `const spk=sc[sc.speaking||'left']`). Do not touch DLGW_IN_* at 3879 — those interior fractions stay correct under either fix.
- risk: low

### [UNVERIFIED] drawCutscene mixes two coordinate systems — portraits in 640-space, panel in viewport-space
- where: game.js:3921
- now: `const s=Math.min(CW/SW, CH/SH), ox=(CW-SW*s)/2, oy=(CH-SH*s)/2;` with SW=640, SH=480 (3898) — a CONTAIN fit, deliberately, per the reverted-0813z note at 3910-3920. Every portrait goes through X()/Y()/S() and therefore lands inside a 682px-wide column centred in the ~910px cinematic frame (s=1.066, ox=114). But the dialogue panel at 4012 uses raw CW fractions and spans the FULL 910px. So the speech panel is 33% wider than the staging area it belongs to, and the left/right portrait slots (3990: left x=12, right x=418 in 640-space) sit well inside the panel's ends. This is a large part of 'clean up all frames'.
- FIX: Pick one space. The least invasive fix is to lay the panel out in 640-space like everything else: `const dx=X(16), dy=Y(343), dw=S(608), dh=S(122)` at 4012 — the panel then aligns with the portrait staging and stops being wider than the scene. Verify against the interior fractions at 3879, which are ratios of dw/dh and stay valid.
- risk: medium — changes the panel's on-screen size on every ensemble scene; worth a screenshot pass

### [UNVERIFIED] There is no comic-panel, gutter or caption-box system anywhere — the only frame decoration is a conditional letterbox
- where: game.js:40559
- now: `if(!beat.text){const bar=Math.max(10,Math.round(H*.052));ctx.fillStyle='#000';ctx.fillRect(0,0,W,bar);ctx.fillRect(0,H-bar,W,bar);}` — black bars are drawn ONLY on textless beats. A dialogue beat has no border at all, and its art is squeezed into the top 70% by 40557 (`cinDrawMotion(hqSc,beat,hqBeatT,W,H*(beat.text?.70:.94))`) with the panel over the bottom 25.5%. So the presentation alternates between a letterboxed action shot and an unbordered squashed one — the inconsistency reads as 'unclean frames'. Grep finds no panel/gutter/balloon/caption-box drawing anywhere in the file; the closest is campaignIntroCaption (40483), a bottom gradient scrim with a yellow head line (#ffe082) and a body block.
- FIX: Build the comic frame as ONE new helper (e.g. cinPanel(x,y,w,h) — hard black gutter, 3-4px black keyline, flat fill) and call it from drawCutsceneBeats around the art area on EVERY beat, textless or not, replacing the conditional bars at 40559. campaignIntroCaption at 40483-40495 is already the closest thing to a yellow narration box and is the natural place to grow the Spider-Man caption panel — it already uses #ffe082 for the head and takes (head, body, alpha).

### [UNVERIFIED] Transition and read timing — every constant that governs 'slow down so we can read'
- where: game.js:40051
- now: `const HQ_CPS=42;` drives the type-on in BOTH cutscene renderers (40564 and 40625). Text beats NEVER auto-advance — they wait for input at 40572, `if(hqBeatT>.18&&(Input.mouse.down||anyTap())){ if(text&&hqChars<n)hqChars=n; else hqAdvance(); }`, so the first tap completes the line and the second advances. The 0.18s guard means a held button can chew two beats in under half a second. Textless beats fade in over 0.24s and out over 0.34s and advance at `beat.duration` (40566-40571). The ensemble path uses a 0.15s guard (40666). Shipped durations are 1.55-3.75s (39822-39889). Sibling systems for comparison: STORY_CPS=46 with STORY_HOLD=1.35 (32312-32313), ARC_CPS=30 (22374), CAMPAIGN_INTRO cross-fade 0.42 (40530).
- FIX: Three one-token changes, all in this window: lower HQ_CPS at 40051 from 42 to ~26; raise the input guard at 40572 from 0.18 to ~0.6 so a completed line cannot be skipped instantly; and add a minimum-hold after the type-on finishes (mirror STORY_HOLD at 32313) before a tap is allowed to advance. Do not raise `duration` values in PILOT_OPENINGS — those are the textless action beats and are already the slowest part.
- risk: low — all four constants are read in one place each

### [UNVERIFIED] Scene-to-boundary table — WHY 'nothing leads up to why we're going to space'
- where: game.js:40026
- now: `const HQ_AT = { pre:{1:'HQ_ALL_00', 8:'HQ_ALL_06'}, post:{1:'HQ_ALL_01', 3:'HQ_ALL_02', 4:'HQ_ALL_03', 6:'HQ_ALL_04', 7:'HQ_ALL_05', 9:'HQ_ALL_07'} };` — eight boundaries for eight scenes. The LAST pre-stage briefing is before stage 8. Stage 9 is Velocity Void, the space/warp stage (the section header at 43483 reads 'STAGE 9 — VELOCITY VOID. Faster motion and space-warp geometry'), and it has NO pre entry — the player is dropped into space with no scene. HQ_ALL_07 'The Emergency Division' (39978-39984) is the only scene that mentions space ('The Signal escaped past the Moon') and it fires POST stage 9, after the player is already there. The space launch itself is only ever depicted in the ending (victorySpaceLaunch, 57390).
- FIX: This is a content gap, not a code bug, and the fix is a table row plus a scene: add `9:'HQ_ALL_08'` to HQ_AT.pre at 40026 and author HQ_ALL_08 in HQ_SCENES (append after 39984) as the launch-to-orbit briefing. hqTrigger (40394) and hqScene (40061) need no change — they are id-driven. Confirm with Mike whether the pre-9 scene should be ensemble (room) or a beats flight sequence, since only the beats path can show the climb.

### [UNVERIFIED] No scene references the level just cleared — there is no per-stage debrief data at all
- where: game.js:39918
- now: All eight HQ_SCENES scripts (39918-39984) are continuity arcs — command authority, a suspected leak, Decker's vault, Juggernaut's reveal. Not one names a stage, a boss, a location or an outcome from the fight the player just finished, even though six of them fire on a POST boundary (40026-40027). The post-clear call site is at 57246, `hqTrigger('post', _st, function(){ openStageSelect(_next, ...) })`, and it passes only the stage number — the scene has no access to what happened. hqPlay (40368) takes (id, onDone) and nothing else.
- FIX: Widen hqPlay's signature to `hqPlay(id, onDone, ctx)` at 40368 and pass the cleared-stage context from 57246 (stage number, boss name, rank). Then a line in HQ_SCENES can carry {STAGE}/{BOSS} placeholders resolved the way subst() already resolves {BOSS_NAME} and {STAGE_NUMBER} for the radio system at 32320-32333 — reuse subst rather than writing a second resolver.
- risk: low — hqPlay has three callers (40404, 40410, and hqTrigger's return at 40410); an optional third arg is backward compatible

### [UNVERIFIED] 'Snapshots of the enemies we destroyed' — no kill record exists, and the cutscene hostiles are three fixed plates
- where: game.js:40073
- now: `const CIN_HOSTILES=['cinhostile_scout','cinhostile_bone','cinhostile_heavy'];` — three plates, registered at 2045-2047 to assets/game/cinematic_ships/hostiles/. cinHostile (40172) picks one by an optional `idx` or by the flip flag; there is no per-stage or per-kill mapping. And nothing records what the player killed: computeStageResults (56291) snapshots only `rows, pct, rank, bonus, face, pw, clearT, seats` — grep for killsByType / run.kills / killLog returns nothing in the file. So a scene cannot currently show what was destroyed because the game never wrote it down.
- FIX: Two-part and sequenced. First, record it: add a `run.killTypes={}` counter incremented wherever an enemy dies, and snapshot it into drawStageClear._res inside computeStageResults at 56291-56303 alongside clearT. Only then can a cutscene panel show 'snapshots'. The art side needs Mike's call — the 0905m note at 2038-2044 says the three cinematic hostiles were generated specifically because the live gameplay enemy plates are strict top-down and clash with the three-quarter hero ships; a per-enemy snapshot set is the same SpriteCook spend he already deferred.
- risk: the art half is a credits decision, not a code one

### [UNVERIFIED] A working nine-slice already exists in this file — the reuse target for un-stretching any panel
- where: game.js:57044
- now: drawStageClear nine-slices `statscreen` inline at 57044-57069: measured margins at 57047 (`const ML=264/1496, MR=270/1496, MT=171/980, MB=171/980`), corner scale `k=Math.min(pw/IW, ph/IH)` at 57053 so brackets never deform, four corners, four edges each stretched on one axis only, then the centre. It is not extracted into a helper. Meanwhile drawPanel (39299) — the helper the pause menu and dlgBox use for dlg_window — is a bare `ctx.drawImage(im, dx, dy, dw, dh)` at 39302-39303 and stretches exactly like the cutscene panels do.
- FIX: Extract 57044-57069 into `function panel9(key, dx, dy, dw, dh, margins)` and call it from three sites: drawCutscene 4019, cinDialogue 40339, and drawPanel 39302. dlg_window's own margins must be MEASURED off the plate the way statscreen's were, not guessed — the note at 3866-3872 warns that a centre-column scan of dlg_window runs past the bottom rail because of the star medallion, and that the bottom must be taken as equal to the top.
- risk: medium — drawPanel is shared with the pause menu and save slots, so change the two cutscene call sites first and drawPanel last

### [UNVERIFIED] Stale comment: the file says cinDialogue is unreferenced; it is the beats renderer's live dialogue path
- where: game.js:39914
- now: The 0904ac header says `⚠ cinDialogue is now referenced by nothing - it was the beats renderer for the openers. Left on disk rather than deleted`. It is wrong: drawCutsceneBeats calls `cinDialogue(beat,hqChars,W,H)` at 40564. The same header block already carries a ⚠⚠ correction at 39909-39913 saying an earlier paragraph in it went stale and 'cost a session's start believing the shootdown beats were dead code' — the cinDialogue line is the next sentence and was missed by that correction.
- FIX: Correct the one line at 39914 to say cinDialogue is the live dialogue renderer for hqMode==='beats', called from drawCutsceneBeats. Anyone doing the comic work will read this block first and would otherwise skip the exact function that draws the dialogue window Mike is complaining about.
- risk: none — comment only

### [UNVERIFIED] The campaign prologue is a separate, fixed-timeline renderer — the only place the premise is explained
- where: game.js:40413
- now: `const CAMPAIGN_INTRO_BEATS=[...]` (40413-40428) is a SEVENTH data shape, unrelated to beats or lines: each entry is {at, to, key, pan, head, body, virus?} — absolute start/end seconds against a single clock, an art key with a pan, and a two-part caption. CAMPAIGN_INTRO_FINALE=24.6, CAMPAIGN_INTRO_DONE=28.7 (40429). drawCampaignIntro (40518) cross-fades between consecutive beats over 0.42s (40530-40534) and draws captions through campaignIntroCaption (40483). This is where the whole premise lives — HQ, the virus, the dispatch — and it is skippable with any button after 0.35s (40542).
- FIX: Leave the timeline shape. For the comic pass this is the best model to copy, not to rewrite: it already has a caption box with a yellow head, hard beat boundaries and a cross-fade. Raise the 0.42 cross-fade at 40530 toward a hard cut (comics cut, they do not dissolve) and gate the skip at 40542 behind a longer stateT than 0.35 so the premise is not lost on an accidental click.
- risk: low

## Cutscene script audit — full text, trigger points, and the narrative gaps

### [UNVERIFIED] WHERE THE SCRIPT LIVES + full scene index (103 scenes, 34 with text)
- where: manifest.js:4
- now: All radio/cutscene dialogue is one generated JSON blob: window.BOFX.story at manifest.js line 4, character offset 451270. Top-level keys: PILOTSELECT, S01..S08, ENDING. Scene inventory (name / when / line count) -- PILOTSELECT: cole, falva, yuri, maverick, decker, axel, freezer, juggernaut, lizzie (3 lines each, when=combat). S01: briefing 6(safe), entry 4(safe), civilian_route_destroyed 5, first_signal_telemetry_discovery 5, miniboss 4, dam_boss 4, completion 7, plus 5 EMPTY stubs (briefing_segment, mid_stage_revelation, miniboss_purpose, boss_purpose, stage_completion_segment). S02: briefing 5(safe), forge_discovery 5, volcanic_drones 5, lavafall_warning 5, boss 4, completion 7, + 5 empty stubs. S03: briefing 5(safe), ancient_fragment_discovery 6, ice_drone_attack 5, yuri_hears_the_countdown 7, boss 5, completion 4, + 5 empty. S04: briefing 7(safe), rival_encounter 5, rival_defeated 4, missile_corridor 4, transmission_discovery 5, boss 4, completion 6, + 5 empty. S05: launch_to_orbit 5, first_alien_contact 4, origin_revelation 5, black_signal_first_clear_message 6, boss 4, completion 6, + 4 empty. S06: reentry 5(safe), missile_attack_from_behind 5, team_sequence 10, boss 4, completion 6, + 4 empty. S07: briefing 5(safe), infrastructure_fusion 4, world_engine_threat 9, cole_and_lizzie 6, boss 4, completion 4, + 5 empty. S08: entry 5(safe), reconstructed_enemies 4, final_form_1_war_machine 4, final_form_2_harbinger 4, final_form_3_fury_mirror 7, final_form_4_furious_death 7, combined_finishing_strike 9, silence_and_transmission 12, + 4 empty. ENDING: ending_card_1..5 and final_sequel_card -- ALL SIX ARE EMPTY (lines:[]).
- FIX: No code change here -- this is the map. Everything below refers back to these ids. If the scenes are rewritten comic-style, this JSON is the single place the text lives; the 33 empty stubs are pre-cut slots waiting for exactly the connective narration Mike is asking for.
- risk: none

### [UNVERIFIED] THE DISPATCHER: only three scene keys are ever requested, out of 103
- where: game.js:32334
- now: function storyPlay(stage, scene) is the ONLY reader of BOFX.story. It is called from exactly three places in the whole file: game.js:25426 storyPlay(num,'entry'), game.js:26260 storyPlay(run.stage,'miniboss'), game.js:26290 storyPlay(run.stage,'boss'). Nothing ever asks for 'briefing', 'completion', or any of the named mid-stage scenes. So 'S01.civilian_route_destroyed', 'S03.yuri_hears_the_countdown', 'S05.origin_revelation', 'S07.cole_and_lizzie', every S08 final-form scene, every PILOTSELECT bark, and all eight completions are authored, shipped in the manifest, and never reachable. This is the mechanical root of 'they make zero sense': the scenes that carry the plot are not the scenes that play.
- FIX: storyPlay itself needs no change -- it resolves any key. Add call sites: (a) a storyPlay(num,'briefing') on the safe pre-stage beat, (b) a storyPlay(run.stage,'completion') on the post-boss slowdown where stageEnding is set, (c) storyPlay(run.stage,<mid id>) on the existing mid-stage progress gates. Cheapest surgical version: give each stage a small ordered scene list keyed off stageTimer fractions and let the existing progress checks near game.js:26260 fire them.
- risk: low

### [UNVERIFIED] ENTRY trigger fires for 2 stages out of 8 (and one of those is suppressed on the normal route)
- where: game.js:25426
- now: Inside beginStage: `if(typeof storyPlay==='function' && run.mode==='campaign' && !(num===8&&run._l78Entry)) storyPlay(num,'entry');`. Only S01 and S08 define an 'entry' key. S02-S07 name their opening scene 'briefing' (S02/S03/S04/S07), 'launch_to_orbit' (S05) or 'reentry' (S06), so the lookup misses and nothing plays. And S08.entry is explicitly skipped when run._l78Entry is set -- which IS the normal route into Stage 8 (game.js:16093/16095/57246-area set _l78Entry=1 on the Stage-7 rift handoff). Net effect in a straight campaign: the ONLY entry exchange a player ever hears is S01.entry, four lines. FULL TEXT of the two that exist -- S01.entry (safe): 1 FALVA 'Those are extraction drones. Private models.' 2 DECKER 'They removed their own identification records.' 3 FALVA 'They can erase the logo. I remember the engines.' 4 COLE 'Then show us where they hurt.' | S08.entry (safe): 1 MAVERICK 'I can see stars through the ground.' 2 FREEZER 'Gravity is shifting.' 3 DECKER 'The crater is becoming a corridor.' 4 AXEL 'All pilots, final weapons check.' 5 COLE 'No reserves. No second mission. We stop it here.'
- FIX: One-line fix in beginStage at game.js:25426: fall back through the alias list instead of the single key -- storyPlay(num,'entry') || storyPlay(num,'briefing') || storyPlay(num,'launch_to_orbit') || storyPlay(num,'reentry'). That alone lights up seven stage openings using text that already exists. Separately, decide whether S08.entry should still be suppressed on the _l78Entry route (it is the only setup the finale gets).
- risk: low

### [UNVERIFIED] MINIBOSS trigger: only Stage 1 has a scene under that key
- where: game.js:26260
- now: `storyPlay(run.stage,'miniboss')` fires on the sub-boss warning. Only S01.miniboss exists. S02-S08 have a 'miniboss_purpose' stub with lines:[] instead, so every other stage's miniboss arrives with no dialogue at all. FULL TEXT of the only one -- S01.miniboss (combat): 1 SYSTEM 'WARNING: HEAVY MECHANIZED CONTACT.' 2 JUGGERNAUT 'Now that looks expensive.' 3 AXEL 'Target the signal growth. Preserve the roadway.' 4 JUGGERNAUT 'You always add that second sentence.'
- FIX: Either author the seven missing miniboss scenes into the existing empty 'miniboss_purpose' stubs and alias the lookup, or accept minibosses are silent outside Stage 1. No renderer change needed.
- risk: low

### [UNVERIFIED] BOSS trigger: a key mismatch silences Stage 1's boss and the entire Stage 8 finale
- where: game.js:26290
- now: `storyPlay(run.stage,'boss')` fires on the boss warning. S02..S07 have a 'boss' key and do play. S01 does NOT -- its scene is named 'dam_boss', so the dam boss is silent. S08 does NOT -- its four boss beats are named final_form_1_war_machine / final_form_2_harbinger / final_form_3_fury_mirror / final_form_4_furious_death, plus combined_finishing_strike and silence_and_transmission, so the FINAL BOSS OF THE GAME plays with zero dialogue and the sequel hook never speaks. UNREACHABLE S01.dam_boss: 1 DECKER 'The control node is inside the boss chassis.' 2 AXEL 'Break the weapons first.' 3 FALVA 'No. Break its balance. The spillway will do the rest.' 4 COLE "Falva's call. Hit the side sections." UNREACHABLE S08.final_form_3_fury_mirror: 1 BLACK SIGNAL 'NINE FUNCTIONS ACQUIRED.' 2 BLACK SIGNAL 'RESISTANCE OBSOLETE.' 3 SYSTEM 'WARNING: FRIENDLY WEAPON PATTERNS DETECTED.' 4 LIZZIE 'It copied our weapons.' 5 COLE 'Weapons, yes. Not the pilots.' 6 AXEL 'Break formation. Fly your own pattern.' 7 MAVERICK 'Best order you have given all day.' UNREACHABLE S08.silence_and_transmission (12 lines, the sequel hook): SYSTEM 'GLOBAL HOSTILE ACTIVITY: ZERO.' / FALVA 'Tell me we won.' / DECKER 'We stopped the Signal.' / YURI 'The countdown ended.' / SYSTEM 'DEEP-SPACE TRANSMISSION RECEIVED.' / BLACK SIGNAL 'SEED FAILURE CONFIRMED.' / 'NATIVE RESISTANCE IDENTIFIED.' / 'HARVEST FLEET REDIRECTED.' / 'ARRIVAL INEVITABLE.' / FREEZER 'That was not surrender.' / COLE 'No.' / COLE 'That was the invasion notice.'
- FIX: Surgical: at game.js:26290 make the boss lookup try the stage's alias -- `storyPlay(run.stage,'boss') || storyPlay(run.stage,'dam_boss')` for Stage 1. For Stage 8 the four forms need firing per form, not once: hook each form transition in the final boss state machine to storyPlay(8,'final_form_N_...') and fire combined_finishing_strike / silence_and_transmission on the kill. That recovers 43 authored lines including the entire ending.
- risk: low

### [UNVERIFIED] THE MISSING RECAPS: all eight 'completion' scenes are authored and none play
- where: manifest.js:4
- now: This is Mike's 'talk about what occured on the previous level' -- it is already written and it is dead. FULL TEXT. S01.completion: FREEZER 'Water level stabilizing.' / FALVA 'Evacuation route is open.' / DECKER 'I have the fragment.' / COLE 'Tell me it is a rock.' / DECKER 'It has internal architecture.' / LIZZIE 'Meaning?' / DECKER 'Comets do not have circuitry.' -- S02.completion: MAVERICK 'Something just launched from the crater.' / AXEL 'Aircraft?' / DECKER 'Negative.' / FREEZER 'Then what is it?' / DECKER 'Alive.' / COLE 'Track it.' / DECKER 'Already leaving the atmosphere.' -- S03.completion: DECKER 'Countdown synchronized with an orbital transmission window.' / COLE 'Then the next node is military.' / AXEL 'Why?' / COLE 'Because it needs an antenna powerful enough to reach whoever sent it.' -- S04.completion: DECKER 'Transmitter destroyed.' / YURI 'Too late.' / SYSTEM 'DEEP-SPACE RESPONSE DETECTED.' / FREEZER 'How far?' / DECKER 'Far enough that the reply should be impossible.' / COLE 'Get the orbital boosters ready.' -- S05.completion: DECKER 'Star map recovered.' / AXEL 'What was the object?' / DECKER 'A Seed. Locate, capture, test, reproduce, summon.' / FREEZER 'Summon what?' / DECKER 'A fleet.' / COLE 'Take the data home.' -- S06.completion: AXEL 'All Fury craft accounted for.' / MAVERICK 'Did you doubt us?' / AXEL 'Constantly.' / FREEZER 'Formation survived.' / AXEL 'Because everyone ignored at least one order correctly.' / FALVA 'He is learning.' -- S07.completion: SYSTEM 'WORLD ENGINE CONNECTION RESTORED.' / DECKER 'Fury Nexus is clear.' / AXEL 'Meridian corridor is still opening.' / COLE 'Then this ends where it began.' Note S02.completion and S03.completion and S04.completion between them are the ENTIRE setup for going to space, and not one of them plays.
- FIX: Add a single storyPlay(run.stage,'completion') on the post-boss slowdown (the branch that sets stageEnding after bossDefeated, around game.js:26290's sibling logic), gated to run.mode==='campaign'. One call site recovers 40 lines of the exact connective tissue Mike says is absent.
- risk: low

### [UNVERIFIED] THE ANSWER ON SPACE: nothing between Stage 4 and Stage 5 says why they launch
- where: game.js:40026
- now: `const HQ_AT = { pre:{1:'HQ_ALL_00', 8:'HQ_ALL_06'}, post:{1:'HQ_ALL_01', 3:'HQ_ALL_02', 4:'HQ_ALL_03', 6:'HQ_ALL_04', 7:'HQ_ALL_05', 9:'HQ_ALL_07'} };` -- this is the complete between-stage schedule. The Stage 4 -> Stage 5 boundary gets exactly one thing: HQ_ALL_03 'The Man Behind the Door', which reads in full: AXEL/COLE 'You entered a vault that does not exist.' / DECKER 'I entered it because it exists.' / AXEL 'Order of the Matrix. You were placed inside Fury.' / DECKER 'The distinction is becoming less clear.' / FALVA 'You used my lab as a bridge.' / DECKER 'Your hardware was the only system the Signal could not recognize.' / DECKER 'Because I found the command buried inside the Signal: HARVEST FLEET REDIRECTED.' Not one word about orbit, boosters, or leaving Earth. The player finishes an airbase level, watches an argument about a vault, and the next thing on screen is Stage 5 in space. There is also NO 'pre' scene for stages 2,3,4,5,6,7 and NO 'post' scene for stages 2, 5 or 8 -- so after the space level clears, and after the FINAL level clears, no between-stage scene plays at all. post:9 targets the secret bonus stage, which sits outside campaign.unlockedMax and most players never see, so HQ_ALL_07 'The Emergency Division' is effectively unreachable too.
- FIX: Two surgical edits, both in this one table. (1) Add post:{5:...} and post:{2:...} entries so the space level and the forge level get a debrief. (2) Add a NEW scene to HQ_SCENES whose subject is the launch decision, and hang it on pre:{5:...} -- it is the single highest-value missing beat in the whole campaign. The trigger plumbing already supports any stage in either slot (hqTrigger at game.js:40394 is called generically from game.js:51111 'pre' and game.js:57246 'post'), so this is a data edit, not a code change.
- risk: low

### [UNVERIFIED] THE EIGHT ENSEMBLE HQ SCENES — full text and exact trigger points
- where: game.js:39918
- now: const HQ_SCENES: eight scenes, format [speaker, poseIndex, text]. HQ_ALL_00 'Nine Chairs, Eight Names' (g.js:39919, plays BEFORE stage 1): COLE 'Every military network on Earth is compromised. Every civilian system connected to it is becoming a weapon.' / AXEL 'Then command authority transfers to the surviving Airforce structure.' / COLE 'There is no surviving structure. There is this room.' / MAVERICK 'Good. Less paperwork.' / YURI 'And less supervision. That is not the advantage you think it is.' / FALVA 'Eight pilots, nine stations. Either Cole cannot count, or somebody is late.' / COLE "That station is not part of today's briefing." / LIZZIE 'Then brief the eight who are.' / DECKER 'The impact was not a comet. It transmitted before it touched the ground.' / FREEZER 'Destination?' / COLE 'Meridian Basin. We cut a road through everything between here and there.' -- HQ_ALL_01 'Trust Is a Weapon' (39932, AFTER stage 1): DECKER 'The dam defense grid was waiting for our exact approach vector.' / AXEL 'That route existed on one closed briefing terminal.' / MAVERICK 'So we have a rat.' / FALVA 'Or the Signal can predict obvious people. You fly like a bar fight.' / MAVERICK 'A winning bar fight.' / YURI 'You broke formation three times.' / COLE 'Nobody leaves the room accused and nobody leaves it cleared. From now on, routes are carried by hand.' / DECKER 'Analog secrecy will slow us.' / COLE 'Staying alive is allowed to take longer.' -- HQ_ALL_02 'Chain of Command' (39943, AFTER stage 3): AXEL 'Your improvisation turned an extraction into a collapse.' / COLE 'Your formation left a burning aircraft outside the shield.' / FREEZER 'One pilot.' / FREEZER 'Then the calculation is wrong.' / LIZZIE 'Enough. Cole chooses the objective. Axel commands the formation. Either man exceeds that boundary, I end the argument.' / MAVERICK 'With diplomacy?' / LIZZIE 'With the payload elevator.' / FALVA 'I vote Lizzie.' -- HQ_ALL_03 (39953, AFTER stage 4) quoted in full above -- HQ_ALL_04 'Open the Ninth Door' (39962, AFTER stage 6): AXEL 'You kept an unregistered heavy pilot beneath an active command base?' / COLE 'I kept a solution that could not be hacked, bribed, reassigned, or remotely activated.' / JUGGERNAUT 'He means I do not take instructions from computers.' / MAVERICK 'I like him.' / YURI 'That is not reassuring.' / JUGGERNAUT 'Brotherhood sent me to protect the thing behind Fury. Not the base. Not the aircraft. The people.' / FALVA 'You have been under us this entire time?' / JUGGERNAUT 'Your music is terrible.' / FALVA 'Open the door. I changed my mind.' / COLE 'Juggernaut, you are cleared for launch.' -- HQ_ALL_05 'Destroy It or Take It Back' (39974, AFTER stage 7): COLE 'The Black Signal is inside the World Engine. We destroy the core and bury everything connected to it.' / LIZZIE 'No. We can destroy it, or we can take it back.' / DECKER 'She is correct. The Signal is not the invasion. It is a change-of-address notice.' / FALVA 'Decker attacks through the network. I keep the counter-signal analog. If one gets infected, the other cuts it loose.' / JUGGERNAUT 'We become the anesthesia.' -- HQ_ALL_06 'Fury Means All of Us' (39981, BEFORE stage 8): COLE 'Axel, the formation is yours.' / AXEL 'And the field command remains yours.' / MAVERICK 'Good. I need you where I am not looking.' / FREEZER 'That may be the closest he gets to trust.' / FALVA 'Then be brilliant faster.' / COLE 'I know.' / JUGGERNAUT 'Doors open. Problems close.' / COLE 'Fury Division—last sky. Launch.' -- HQ_ALL_07 'The Emergency Division' (39991, AFTER stage 9, the secret bonus stage): DECKER 'The Signal escaped past the Moon.' / FALVA 'Show us.' / LIZZIE 'This was supposed to be the emergency division.' / COLE 'It still is.' / COLE 'It does now.' Note HQ_ALL_06 and HQ_ALL_07 both contain orphan lines: MAVERICK 'Good. I need you where I am not looking.' has no antecedent, COLE 'I know.' answers nothing, and COLE 'It does now.' answers nothing -- these read as scenes trimmed mid-edit, which is a concrete part of 'make zero sense'.
- FIX: None of these eight mention the stage that just ended in concrete terms -- they are character-relationship scenes, not debriefs. To meet 'talk about what occured on the previous level', either prepend a caption/narration beat to each (the yellow-box role in Mike's Spider-Man reference) or route the matching SNN.completion scene to play immediately before the HQ scene at the same boundary, via the existing onDone chain in hqTrigger (game.js:40394). Also fix the three orphan lines in HQ_ALL_06/07.
- risk: low

### [UNVERIFIED] THE NINE PER-PILOT OPENINGS — full text, and they play only before Stage 1
- where: game.js:39820
- now: const PILOT_OPENINGS, nine entries, each a beat list of {motion, bg, duration} action shots and {who, motion, text} dialogue beats. hqTrigger (game.js:40394) plays these ONLY at when==='pre' && stage===1, then hands off to HQ_ALL_00. FULL DIALOGUE. axel (39821, cast axel+freezer, ends at HQ): AXEL 'You okay over there, bud? What the hell was that back there?!' / FREEZER "No idea, man. It's like they all started acting like they had minds of their own and took over out of nowhere! We've got to warn the others, but our comm systems are down. The bastards probably took our whole operation down!" / AXEL "SH*T! We've got to get back to base, NOW!" -- freezer (39828, ends at jungle): FREEZER 'Axel, you okay over there? My comm system is completely dead.' / AXEL 'I am good. Whatever hit us took the whole operation dark. We need to get back to base.' / FREEZER "Wait. My radar is still working. I'm picking up a strong Fury-class unit in the jungle ruins just off the coast. Maybe it's one of the others." / AXEL 'Then we find them. Lead the way.' -- cole (39836): COLE "Did you think you were dealing with an amateur over here?! You're gonna have to try better than that, you clankerf**ks!" / COLE 'I hope the others are doing okay... I better head back to base and try to find them.' -- falva (39843, cast falva+lizzie): FALVA 'That was a close one! Sis, where did they even come from?!' / LIZZIE "No idea, but they picked the wrong ones, that's for sure. We've got to find the others before more show up!" / FALVA 'Our comms system seems to be down... radar still seems to be working. I am detecting anomalies in the jungle just off the coast. Let us check it out!' / LIZZIE 'I got your back, girl. Over and out!' -- maverick (39851): MAVERICK "That was close... I thought I told these clowns money won't do any good in the bank if I'm dead. Why isn't HQ returning my calls either...?" / MAVERICK "Huh... that's strange. I know I'm an independent contractor and all, but part of the gig was returning to a base that's still erect. Just when I thought I was out, they pull me back in!" -- yuri (39857, traffic axel/cole/falva): YURI 'What the...? Everyone is burning back toward HQ like the sky is falling.' / YURI 'My comms are down too. Something is very wrong. I am following them.' -- juggernaut (39863, cast juggernaut+yuri): JUGGERNAUT "I'M THE JUGGERNAUT, B1**H!" / YURI 'Thank God I found you! Any idea why the others are all flying toward HQ?' / JUGGERNAUT 'No clue, kid! Did you try calling them?' / YURI 'Perhaps it is just me, but the comms system appears to be down.' / JUGGERNAUT "Well... that sure isn't good... F**K! WE GOTTA GO NOW! I'M THE JUGGERNAUT!!!!!!!" -- lizzie (39872, cast lizzie+falva): LIZZIE 'That is how you clear a sky. You still with me, sis?' / FALVA 'Barely! Where did they even come from?' / LIZZIE "Doesn't matter. They picked the wrong sisters. We find the others before round two starts." / FALVA 'Comms are down, but radar has an anomaly in the jungle just off the coast.' / LIZZIE 'Then stay on my wing. We are going in.' -- decker (39881, cast decker+maverick): MAVERICK "Say, you wouldn't be able to cloak me up before these nasty hostiles show up again, would you?" / DECKER 'Maybe back at base that could be possible, but right now it appears our comms system is down.' / MAVERICK 'Ten steps ahead of you. Any idea why the fridge at base tried to eat Larry the Janitor?' / DECKER '...' / MAVERICK '...' / DECKER 'We need to head to base, right away!' CONTINUITY BUG: freezer, falva and lizzie all set destination:'jungle' and end on a 'jungle_approach' beat -- but the next thing that plays is HQ_ALL_00, which is set in the HQ briefing room. Those three openings fly to the jungle and then cut to a room they never arrived at.
- FIX: Two fixes. (1) The destination:'jungle' mismatch: either change those three to destination:'hq' with an hq_approach final beat, or have hqTrigger skip HQ_ALL_00 for jungle-bound pilots. Edit is in PILOT_OPENINGS (game.js:39828/39843/39872) or in the pre-stage-1 branch of hqTrigger (game.js:40394). (2) These openings are the only cutscene in the game that already IS the format Mike asked for -- top-down/pseudo-3D ships flying next to each other cutting to dialogue windows with avatars (drawCutsceneBeats, game.js:40551; cinFormation and cinDrawMotion carry the depth pass). Whatever comic-panel treatment gets built should extend THIS renderer rather than the ensemble one.
- risk: low

### [UNVERIFIED] THE PROLOGUE that does explain the premise — plays once, only on a brand-new campaign
- where: game.js:40413
- now: const CAMPAIGN_INTRO_BEATS, seven timed caption cards over painted backdrops, total 28.7s (CAMPAIGN_INTRO_DONE). FULL TEXT, head then body. 1 'FURY HQ - EARTH DIVISION' / "Hidden off the coast, FURY HQ is home to Earth's strongest elite pilots." 2 'THE HIDDEN FORTRESS' / 'Mercenaries, former militia and specialists without official affiliation were chosen for one reason: defend the globe.' 3 'THE LAST LINE OF RESISTANCE' / 'The United States government formed FURY for secret and covert missions - and to fight back if an enemy ever seized control.' 4 'NINE PILOTS. ONE MISSION.' / 'Different histories and different loyalties, united against every known and unknown threat to our world.' 5 'THEN, WITHOUT WARNING...' / 'One night beneath the darkened sky, a digital virus began spreading through every network it touched.' 6 'SYSTEMS COMPROMISED' / 'Vehicles. Aircraft. Automobiles. Entire defense systems turned hostile, moving as though they possessed minds of their own.' 7 'ONE FINAL DISPATCH' / 'With no explanation and one last call for help, the team launched to learn what happened, why it happened - and who did this.' Trigger: startRun at game.js around 24517 (campaignIntroStart) fires it only when campaign._booted is false AND fromStage===1 -- so a Continue, a password start, or a revisit never sees it. It is skippable with any tap from stateT>0.35.
- FIX: This is the ONLY sequence in the build that states the premise, and it is a caption-over-still slideshow -- exactly the presentation Mike is calling non-comic. It is also the natural model for the yellow narration box he wants: campaignIntroCaption() already renders head+body and is reused by the victory ending (victoryCaption, game.js near 57290). Restyle campaignIntroCaption once and every narration card in the game changes with it.
- risk: low

### [UNVERIFIED] ARCADE CARDS: the per-stage recap narration exists, and half of it is never drawn
- where: game.js:32568
- now: arcStageCard(stage, which) resolves BOFX.arcade.stage_cards_and_completion_cards -- 41 authored cards, four per stage: __intro, __mid_stage, __boss_warning, __complete. Only TWO of the four are ever requested: 'intro' at game.js:53782 (drawIntro, drawn under the stage card) and 'boss_warning' at game.js:26289. 'mid_stage' and 'complete' have NO call site, so 16 cards never render -- and the eight '__complete' cards are precisely the previous-level recap Mike is asking for. UNUSED __complete TEXT: S1 'THE FRAGMENT CONTAINS CIRCUITRY. / THE COMET IS NOT NATURAL.' S2 'THE SEED BROUGHT NO ARMY. / IT BROUGHT INSTRUCTIONS.' S3 'A COUNTDOWN IS ACTIVE. / SOMETHING IN SPACE IS WAITING.' S4 'THE TRANSMITTER IS DOWN. / THE ANSWER HAS ALREADY ARRIVED.' S5 'THE COMET WAS ONLY A SEED. / THE HARVEST FLEET IS COMING.' S6 'FURY HAS RETURNED. / THE SIGNAL IS ALREADY INSIDE THE BASE.' S7 'FURY NEXUS IS SAVED. / THE MERIDIAN GATE IS OPENING.' S8 'THE BLACK SIGNAL IS SILENT. / DEEP-SPACE TRANSMISSION DETECTED.' UNUSED __mid_stage TEXT: S1 'THE ENEMY IS RECORDING EVERY FURY ATTACK.' S2 "EARTH'S FACTORIES ARE BUILDING THE ENEMY'S ARMY." S3 'THE SIGNAL WAS HERE THOUSANDS OF YEARS AGO.' S4 'FURY COMBAT DATA IS BEING SENT INTO DEEP SPACE.' S5 "ALIEN SOLDIERS HAVE BEEN GROWN FROM EARTH'S OWN MATERIALS." S6 'NINE PILOTS. ONE FORMATION.' S7 'THE WORLD ENGINE IS BECOMING A WARP GATE.' S8 'THE ENEMY HAS REBUILT EVERYTHING FURY DESTROYED.' Also unused: the four Stage 8 boss_form_1..4 cards, S4 rival_warning, S6 rear_warning, S5 signal_message, and both BOFX.arcade.new_game_opening cards and all six BOFX.arcade.arcade_ending cards (arcCard is only ever called for 'attract_mode' at game.js:32636 and 'game_over_variants' at 32566-32570). Stage 3's card literally says 'SOMETHING IN SPACE IS WAITING' and Stage 5's intro says 'FURY ENTERS ORBIT. THE BLACK SIGNAL IS CALLING HOME' -- the space setup was written as captions and the recap half was never wired up.
- FIX: Draw the '__complete' card on the stage-clear/debrief screen and the '__mid_stage' card at the existing mid-stage progress gate, using the same arcDraw path drawIntro already uses at game.js:53782. Two call sites, no new text, and it directly answers 'talk about what occured on the previous level'. These short all-caps two-liners are also already shaped like Spider-Man narration boxes.
- risk: low

### [UNVERIFIED] THE ENDING: the campaign finale is a caption slideshow, and the authored ENDING scenes are empty
- where: game.js:57290
- now: The victory sequence is VICTORY_PILOT_ENDINGS (nine one-shot pilot captions) plus five timed beats: victoryRestoration (pilot caption), victoryDish at 57355, victoryWarning at 57377, victorySpaceLaunch at 57390, victoryStinger, victoryFinalCard at 57421. FULL CAPTION TEXT: pilot card, e.g. COLE 'COLE - AIRSPACE CLEAR' / 'Last clanker is scrap. Airspace is ours, systems are clean, and Earth gets another sunrise. Not bad.' (all nine at game.js:57290-57300). Then 'THE FINAL BATTLE WAS OVER...' / 'But a piece of the alien symbiote survived. Hidden. Watching. Waiting.' Then 'FURY COMMAND' / 'Pilot, we just received an emergency transmission from the Space Federation.' Then 'SPACE FEDERATION - PRIORITY CHANNEL' / 'Something BIG has happened beyond the outer system. If we do not stop it, it is heading for Earth - fast.' Then 'UNKNOWN CONTACT' / 'Trajectory confirmed. Deep-space approach vector: EARTH.' Then a shadowed sequel warform, then 'THE END' / 'BULLETS OF FURY,' / 'WILL RETURN!'. Meanwhile BOFX.story.ENDING.ending_card_1..5 and final_sequel_card all exist with lines:[] -- empty -- and BOFX.arcade.arcade_ending has six FULL cards that nothing calls: 'THE SEED WAS DESTROYED. / EARTH'S MACHINES FELL SILENT.' / 'BUT THE COMET WAS NOT THE INVASION. / IT WAS THE ANNOUNCEMENT.' / 'HARVEST FLEET REDIRECTED. / ARRIVAL INEVITABLE.' / 'FURY DIVISION WILL NOT WAIT.' / 'NEXT DESTINATION: THE UNKNOWN.' / 'BULLETS OF FURY II / LOST CONQUEST / THE WAR FOR EVERYTHING HAS BEGUN.' So the ending the player sees is a Space Federation subplot that appears nowhere else in the campaign, while the ending that ties back to the Black Signal's own 'HARVEST FLEET REDIRECTED' line sits unused in two places.
- FIX: Fill the six empty BOFX.story.ENDING scenes from the arcade_ending cards, or point the victory beats at arcCard('arcade_ending', ...) instead of the hardcoded Space Federation captions. Either way the finale then pays off the S08 Black Signal lines and HQ_ALL_03's 'HARVEST FLEET REDIRECTED' instead of introducing a brand-new faction in the last thirty seconds.
- risk: low

### [UNVERIFIED] PILOTSELECT barks — nine scenes, 27 lines, no dispatcher
- where: manifest.js:4
- now: BOFX.story.PILOTSELECT holds three lines per pilot, all when='combat'. Nothing calls storyPlay('PILOTSELECT', <pilot>) anywhere in game.js -- the only three storyPlay call sites are the entry/miniboss/boss ones. FULL TEXT: COLE 'Cole Force 1 online. Let us get to work.' / 'If it has a system, it has a weakness.' / 'Forge Master ready. Open the hangar.' -- FALVA 'The OG is airborne. Try to keep up.' / 'Roller Ball charged. Point me at the problem.' / "Chad ready. Yes, that's still the callsign." -- YURI 'Lightning ready. The storm is listening.' / 'Give me a circuit and I will break the whole chain.' / 'The sky has chosen a side. So have I.' -- MAVERICK "Venom loaded. Let's make this look impossible." / 'Nine pilots and you picked the fun one.' / 'Targets ahead? Good. I hate long briefings.' -- DECKER 'Data online. Their systems are already exposed.' / 'Cloaking stable. They cannot kill what they cannot locate.' / 'I have reviewed the odds. They were unhelpful.' -- AXEL 'Caliber ready. Form on me.' / 'Shield of God online. Nobody falls today.' / 'Discipline, distance, fire. Move.' -- FREEZER 'Yeti online. Slow down. Aim once.' / 'Time is relative. Their survival will be brief.' / 'Cold systems. Clear mind. Ready.' -- JUGGERNAUT 'Unstoppable is ready to stop something.' / 'Weapons loaded. Hull loaded harder.' / 'Open the door before I make one.' -- LIZZIE 'B-1942 ready. Ordnance secure.' / 'Atomic systems armed. Nothing leaves the target zone.' / "I'll carry the payload. You clear the road." Note storyPlay's rule-7 filter (storySpeakerIsPlayer, game.js just above 32334) DROPS any line whose speaker is the selected pilot -- so even if a call site were added at pilot select, every one of these 27 lines would be filtered out and the scene would return false.
- FIX: If these are wanted, the call site must bypass the rule-7 filter: add an opt-out flag to storyPlay (e.g. storyPlay(key, scene, {allowSelf:true})) and skip the `lines.filter(l=>!storySpeakerIsPlayer(l[0]))` line inside it, then call it from the pilot-select confirm. Without the flag the scene silently no-ops -- this is a trap worth knowing before anyone 'just adds the call'.
- risk: low

### [UNVERIFIED] TWO RENDERERS, TWO LOOKS — why the cutscenes do not read as one presentation
- where: game.js:40551
- now: GS.CUTSCENE runs one of two completely different renderers chosen by hqMode. drawCutsceneBeats (game.js:40551, hqMode='beats') is the per-pilot opening: painted backdrop, pseudo-3D ships via cinDrawMotion/cinFormation with depth cues, letterbox bars on action beats, and a dialogue panel from cinDialogue that uses the speaker's own tinted dlg_<pilotkey> plate at x=W*.025 y=H*.715 w=W*.95 h=H*.255 -- a full-width box stretched to 95% of the frame. drawCutsceneEnsemble (game.js:40577, hqMode='ens') is the eight HQ scenes: a static 16:9 room plate cropped at bgZoom 1.16, no ships, no motion, one dialogue frame via drawCutscene. Type speed differs from the in-play radio too: HQ_CPS=42 (game.js:40051) vs STORY_CPS=46 with STORY_HOLD=1.35s (game.js:32312-32313). The in-play radio uses a THIRD renderer, dlgBox. So a campaign run shows the player four visually unrelated dialogue treatments (beats panel, ensemble panel, in-play dlgBox, caption cards) in the first ten minutes.
- FIX: For the comic-panel rework: the beats renderer is the one to build on (it already does top-down/pseudo-3D ships plus cut-to-dialogue with avatars). Concretely -- (a) stop stretching the box: cinDialogue's dw=Math.round(W*.95) is the 'stretched dialogue box' Mike names; give the new SpriteCook cinematic window a fixed aspect and letterbox it instead of scaling to 95% width. (b) Slow the reads: HQ_CPS=42 at game.js:40051 and STORY_HOLD=1.35 at game.js:32313 are the two numbers that control how long text sits on screen. (c) Make the ensemble scenes use the beats renderer with ship motion so the two stop looking like different games.
- risk: medium

## Cutscene enemy snapshots — kill record + still-portrait rendering

### [UNVERIFIED] The kill record Mike needs DOES NOT EXIST — stageStats is scalars only
- where: game.js:7273
- now: stageStats = {kills, shots, hits, livesStart, scoreStart, spawned, deaths, missiles, dmgDealt, dmgTaken, mslHits, spShots, spHits, spDmg, pickups, pickupsSeen, wpn:{}}. `kills` is a bare integer. The only per-KEY accumulator in the whole object is `wpn:{}` (per-weapon damage, added in drop 0904y). stageStats2 (line 7298) mirrors it for P2. I grepped the entire 58k-line file for stageStats / kills / killedTypes / bestiary / defeated / seenEnemies / killLog and for any `[e.type]=` accumulator: the ONLY per-type map in the file is INVIS_BLOCKED (line 9713), which counts spawn-time art failures, not kills. There is no record anywhere of WHICH enemy types the player destroyed.
- FIX: Add one field to the stageStats literal at 7273 and the identical literal at 7298: `foes:{}`. Add the same field to the two reset literals in beginStage (line 25326 for stageStats, 25341 for stageStats2) — the file's own comment at 25321 warns that the 0807o drop extended the declaration and missed the reset, leaving four fields undefined forever. Do not add anything else here.
- risk: The reset literal is a KNOWN trap documented in-file. Editing 7273 without 25326 gives a field that reads undefined from the first stage onward.

### [UNVERIFIED] killFeedback() is the single chokepoint where a killed-type record should be written
- where: game.js:4223
- now: `function killFeedback(e, sc)` — floats the score text and adds a ground scorch. Its header comment at 4214 says verbatim: "ONE PLACE THAT ANSWERS 'I KILLED SOMETHING' (drop 0822g). Kills awarded score in three separate branches and NONE of them told the player". It receives the whole enemy object `e`, so e.art, e.type, e._nef, e._blk and e._dr are all in hand. It is called from all four kill branches in killEnemy: 28382 (water rock), 28396 (bunker/turret collapse), 28416 (the modern art path every current enemy takes), 28455 (plain dead).
- FIX: Add one guarded line at the top of killFeedback, after `if(!e) return;`: record the enemy's PORTRAIT KEY into stageStats.foes, e.g. `try{ if(typeof stageStats!=='undefined' && stageStats.foes){ const k=enemyPortraitKey(e); if(k) stageStats.foes[k]=(stageStats.foes[k]||0)+1; } }catch(_fq){}` — same defensive shape as the wpn tally at 28358. Do NOT scatter writes across the four killEnemy branches; that is the exact mistake the _dmgSrc comment at 7282 says has cost this codebase repeatedly (~40 stageStats sites, editing each is how one gets missed).
- risk: Branch 28382 is the water-rock/scenery kill. Line 49319 carries an explicit note that scenery is not counted as kills, so a scenery guard (`if(e._waterRock||e._prop) return;`) belongs in the recorder or Mike's panel will show exploding barrels as defeated foes.

### [UNVERIFIED] The art key that renders any enemy type as a static portrait: ENEMY_ART[art] + '_idle'
- where: game.js:31636
- now: drawNewEnemyArt does exactly `const base=ENEMY_ART[e.art]; if(!base) return false;` then `let key=base+'_'+enemyArtState(e)` and falls back to `base+'_idle'` if that state key is not ready. ENEMY_ART itself is at 31419 — it maps an art NAME (tankA, jet1, s1tankHeavy, xelite_furytalon, miniboss1) to a base key (tnkG_g2, air_air1, s1_tank_heavy, xelite_furytalon, esB_big1). enemyArtState (31460) returns idle/fire/death/wreck; 'idle' is the clean standing frame. The spawn-time visibility gate at 10361 uses `XART._src[_b+'_idle']` as its literal definition of "will this unit draw at all", so `<base>_idle` is the engine's own canonical still frame.
- FIX: No change to this function. Add a small standalone resolver near the cinematic code (beside cinHostile at 40172), e.g. `function enemyPortraitKey(e)` returning, in this order: (1) `e._dr && e._dr.slug` -> 'ndr_'+slug+'_idle_0'; (2) `e._nef` -> e._nef+'_intact'+(e._blk?'_blk':'')+'_idle'; (3) `ENEMY_ART[e.art]` -> that base +'_idle'; else null. Three lines, no rewrite of any draw path.

### [UNVERIFIED] Verified: every ENEMY_ART base has a matching _idle key in the shipped manifest
- where: manifest.js:4
- now: window.BOFX (manifest.js line 4) carries the img and cells tables. I extracted all 31 distinct base values from the ENEMY_ART literal (game.js 31419-31459) and grepped each `<base>_idle` against manifest.js: zero missing. Spot-verified individually: tnkG_g2_idle, air_air1_idle, s1_tank_heavy_idle, s1_jet_delta_idle, xelite_furytalon_idle, esB_big1_idle, nef_s3_aa_sled_intact_idle, nef_s1_jungle_tank_intact_idle, nef_s1_jungle_tank_intact_blk_idle, nef_s3_aa_sled_damaged_idle, nef_s3_aa_sled_critical_idle — all present.
- FIX: Nothing to change. This is the proof that a portrait panel needs NO new art for the roster Mike is asking to show — the still frames already ship. Only the three pseudo-3D cinhostile plates were ever new art.

### [UNVERIFIED] XART.draw() is already the exact still-image drawer a comic panel needs
- where: game.js:2621
- now: `X.draw=function(k,cx,cy,w,alpha)` — touches the key (lazy-decodes it), returns null if not ready, derives height from the image's own aspect, centres on (cx,cy), rounds to whole pixels, honours alpha, and returns {w,h}. X.rdy is at 2588, X.get at 2586 (returns a drawable blank rather than null). XART._touch (2549) resolves atlas cells and loose images identically, so a portrait key works whether the frame lives in BOFX.cells or BOFX.img.
- FIX: A cutscene panel renders a defeated foe with one call: `XART.draw(portraitKey, panelCx, panelCy, panelW*0.7, 1)`. Do NOT call drawEnemy/drawNewEnemyArt for this — those read e.x/e.y, e.flash, e._navBob, volcHover and the live world transform, and drawNewEnemyArt returns early (`if(e._dyingT!=null) return true`) for anything dying.

### [UNVERIFIED] TRAP: nef_ units mutate e.art with hp, so the art name at kill time is a WRECKED hull
- where: game.js:31914
- now: drawEnemy's first statement is `if(e._nef) e.art=nefArtFor(e);`. nefArtFor (35297) returns `base+'_'+state+(e._blk?'_blk':'')` where state is intact/damaged/critical by hp fraction. So for the whole stage-1/2/3 roster, e.art at the moment of death is typically `nef_s1_jungle_tank_critical` — a burning hull, not a portrait. The clean base is stashed separately at 35317: `c._nef=d.art; c._blk=!!d.blk;` (e.g. 'nef_s1_jungle_tank').
- FIX: In the portrait resolver, branch on `e._nef` BEFORE `e.art` and rebuild the intact key: `e._nef+'_intact'+(e._blk?'_blk':'')+'_idle'`. Verified in manifest: bare `nef_s3_aa_sled` is NOT a key (0 hits) — the state suffix is mandatory.
- risk: If Mike wants the panel to show the wreck rather than the intact unit, use e.art as-is instead; both `_damaged_idle` and `_critical_idle` aliases exist. Worth asking him — a comic 'snapshot of what we destroyed' arguably wants the burning frame.

### [UNVERIFIED] TRAP: arsenal drones bypass ENEMY_ART entirely, and their art helper is unreachable from cutscene code
- where: game.js:8937
- now: droneDraw builds its key inline: `const k='ndr_'+slug+'_idle_0';` from `e._dr.slug`. These types have no ENEMY_ART entry at all — the spawn gate at 10358 checks `XART._src['ndr_'+c.type+'_idle_0']` as a separate first case. The obvious helper, arsenalDroneArt, is declared at 9342 but the ⚠ comment at 9355 states it and ARSENAL_DRONES are STILL function-scoped inside spawnEnemy (whose `if(base.art===undefined){` is never closed), so anything outside spawnEnemy that reads them silently gets undefined.
- FIX: Build the drone key inline in the portrait resolver exactly as droneDraw does — `'ndr_'+e._dr.slug+'_idle_0'` — and never call arsenalDroneArt from cinematic code. That is 12 enemy types (ARSENAL_DRONES, 9336: stages 2, 3, 5 and 8) that would otherwise silently render nothing in a panel.

### [UNVERIFIED] cinHostile() is the one existing function that draws a hostile inside a cutscene — and it is hardcoded to three plates
- where: game.js:40172
- now: `function cinHostile(x,y,h,flip,alpha,rot,idx)` picks from CIN_HOSTILES (40073) = ['cinhostile_scout','cinhostile_bone','cinhostile_heavy'], falls back to whichever of the three is decoded, and draws it centred with flip/rot/alpha. The beats can only ever show these three generic ships — nothing to do with what the player actually killed.
- FIX: Widen the last parameter: accept a STRING as an explicit art key (`if(typeof idx==='string') key=idx;`) alongside the existing numeric index, so a beat can pass a recorded portrait key from stageStats.foes. The existing numeric/flip behaviour is untouched, so every current call renders identically — the same compatibility discipline the 0905m `idx` addition already used.

### [UNVERIFIED] Timing verified: the post-stage cutscene reads the stats of the stage that just finished
- where: game.js:57246
- now: `hqTrigger('post', _st, function(){ openStageSelect(_next, ...) })` fires from the stage-clear/debrief screen, keyed on _st (the stage COMPLETED, per the comment at 57242). hqTrigger (40394) reads HQ_AT.post (40027) = {1:'HQ_ALL_01', 3:'HQ_ALL_02', 4:'HQ_ALL_03', 6:'HQ_ALL_04', 7:'HQ_ALL_05', 9:'HQ_ALL_07'}. stageStats is reset ONLY inside beginStage, at line 25326, which has not run yet. The pre-stage trigger at 51111 (`hqTrigger('pre', _st, function(){ beginStage(_st); })`) also runs before the reset, so a 'pre' scene still sees the PREVIOUS stage's record.
- FIX: No change needed for the post-stage case — stageStats.foes is live and correct at hqTrigger('post'). Mike's 'talk about what occurred on the previous level' works off stageStats directly.
- risk: Fragile if a scene is ever entered from the campaign map after a reload. For that, mirror the record onto the persistent structure — see the run/campaign finding below.

### [UNVERIFIED] Existing precedent AND the durable home: run._lastBossName
- where: game.js:28773
- now: bossDie() captures `run._lastBossName=String(boss.name)` with the comment: "the debrief names the boss you just beat, and by then the object is gone - so the name is taken here, at the one moment it is certainly correct, rather than re-derived from a kind." It is read once, at 56864, by the stage-clear brief ('BOSS DEFEATED = ...'). `run` is a single `const run = {...}` at 7163 that is mutated, never re-created, so fields on it survive the stage boundary and the cutscene.
- FIX: This is the exact pattern to copy, and it also answers 'where should the record live if it must survive'. If a cutscene ever needs a foe list after a save/reload, mirror stageStats.foes into `campaign.foes[stage]` beside campaign.rank (declared 49787, snapshotted 49810, restored 49841). The comment at 49803 notes CAMP_SAVE_VER need NOT be bumped for an optional new field — an older slot simply has no value.

### [UNVERIFIED] Bosses are a SEPARATE art family — ENEMY_ART does not reach them
- where: game.js:19586
- now: Minibosses/bosses draw from `b.art+'_idle'` (19586) or `b.art+'_'+state` (19591); mega bosses use `b.art+'_inta'` (1748-1749, 2751). Boss art names are set directly on the boss object (18214-18234, e.g. b.art='sub_tank'), and the shipped plates are the nsb_ family with _intact/_damaged/_critical states (see _bossplates.txt at the repo root). killEnemy/killFeedback never run for a boss — bossDie (28766) is its own path.
- FIX: If Mike wants the boss in the 'what we destroyed' panel (he almost certainly does — it is the level's payoff), add a second one-line capture in bossDie beside the existing name capture at 28773: `try{ if(boss&&boss.art) run._lastBossArt=String(boss.art); }catch(_ba){}` and resolve the panel key as `run._lastBossArt+'_idle'` with `_inta` / `_intact` fallbacks via XART.rdy.
- risk: Boss key suffixes are inconsistent across families (_idle, _inta, _intact). The panel must try each with XART.rdy rather than assuming one.

### [UNVERIFIED] Context Mike's new brief reverses: the 3/4 pseudo-3D hostile plates exist because game art is top-down
- where: game.js:2030
- now: The ⚠ comment at 2030 (drop 0905m, backlog item 2) records that cinhostile_scout/bone/heavy were generated as NEW three-quarter plates precisely because borrowing live gameplay sprites looked wrong: "These three keys BORROWED live gameplay sprites from stages 5 and 8, and every one of those is STRICT TOP-DOWN - so the beats drew flat top-downs next to `cinship_*`, which are authored front/rear three-quarter pseudo-3D." The comment also warns the old file paths were deliberately left alone because they are live enemy reels referenced elsewhere.
- FIX: No code change — this is the decision record that makes the new brief cheap. Mike now asks for cutscenes that are "top down like in-game with vertically scrolling" and "their top down jets flying next to each other", which REMOVES the perspective mismatch that forced those three plates. Drawing real ENEMY_ART _idle frames in a panel is now consistent with the requested look rather than fighting it, and needs no SpriteCook credits for the enemies themselves. The hero ships would need matching top-down frames though — cinship_* are 3/4 only.
- risk: cinship_<pilot>_1..7 are the only cutscene ship art and they are three-quarter. A top-down two-jets-side-by-side beat needs the in-game player ship art (nsa_ships) instead, which is a separate question from this cluster.

## cutscene-dialogue-window-sizing

### [ADJUSTED] Cutscene dialogue frame is drawn at an arbitrary, non-uniform scale (per-pilot opening / beats renderer)
- where: game.js:40326
- now: cinDialogue (assets/game.js:40324) builds its rect at line 40326 from raw viewport fractions with no reference to the art: `const dx=Math.round(W*.025),dy=Math.round(H*.715),dw=Math.round(W*.95),dh=Math.round(H*.255);` then blits the plate into it with a 6-arg drawImage at line 40339 (`ctx.drawImage(XART.get(_dk),dx,dy,dw,dh)`). W=cutsceneViewWidth()=CINEMA_VW, H=VH=512 (VW/VH declared assets/game.js:14; CINEMA_VW computed assets/game.js:6096 as Math.max(640,Math.round(VH*ar))). So dh is ALWAYS 131px and dw floats with the browser. On a 16:9 window CINEMA_VW=910, dw=865, dh=131 -> drawn aspect 6.60:1. Source dlg_window is 1465x808 = 1.813:1; the dlg_<pilot> plates are 569x321 = 1.773:1. x-scale 865/1465=0.590 against y-scale 131/808=0.162, i.e. 3.64x wider than tall (3.73x for a pilot plate, simultaneously MAGNIFIED 1.52x horizontally and CRUSHED to 0.41x vertically). Even at the floor CINEMA_VW=640 the distortion is still 2.56x. This is exactly Mike's 'do not stretch dialogue boxes'.
- FIX: In cinDialogue (assets/game.js:40324-40352), stop deriving dh from H. After `_dk` is resolved at line 40338, size the rect from the chosen plate: keep dy/dh as the maximum band, compute a uniform k=Math.min(maxW/im.naturalWidth, maxH/im.naturalHeight), set dw=im.naturalWidth*k, dh=im.naturalHeight*k, and centre horizontally (dx=Math.round((W-dw)/2)), bottom-anchoring to the old band. Factor into one helper (e.g. cinPanelRect(imgKey,W,H)) and call it from here AND from drawCutscene:4012. Guard it on XART.rdy - XART.get returns a 1x1 _BLANK CANVAS when not decoded (assets/game.js:2440,2577) and a canvas has no .naturalWidth, so an unguarded read yields NaN. NOTE FOR THE SPRITECOOK BRIEF: at 1.813:1 an aspect-correct panel filling 95% of a 910-wide cinema view is 477px tall, 93% of the 512 screen. New cinematic windows must be authored WIDE - roughly 4:1 to 6.5:1 (e.g. 1280x320 or 1600x256) - or the helper letterboxes them into a much smaller centred box.
- risk: MOSTLY as the report says, with one correction. The interior POSITIONS are fractions of dw/dh (padX=dw*.055, nameY=dy+dh*.115, bodyY=dy+dh*.34, bodyH=dh*.52 at lines 40340/40343) and do follow the frame. But the text SIZES are fractions of the SCREEN, not the panel: nameH=Math.max(12,Math.round(H*.030)) at 40340 and maxH=Math.max(13,Math.round(H*.031)) at 40345. They will not scale with the panel, so a much shorter panel would keep full-size type. Growing dh is safe; shrinking it is not. Land this with the new wide SpriteCook plates, not before them - line 40563 confines the ships to the top 70%, so a 1.8:1 panel would cover them.

### [CONFIRMED] The ensemble / room cutscene renderer repeats the identical stretched rect
- where: game.js:4012
- now: drawCutscene (starts assets/game.js:3894) has a byte-identical copy of the same maths at line 4012: `const dx=Math.round(CW*.025), dy=Math.round(CH*.715), dw=Math.round(CW*.95), dh=Math.round(CH*.255);` where CW=cutsceneViewWidth() and CH=VH (line 3898). The plate is drawn with the same 6-arg stretch at line 4019 (`ctx.drawImage(XART.get('dlg_window'), dx, dy, dw, dh)`), same 3.64x horizontal distortion as cinDialogue. The surrounding function is otherwise scrupulous about uniform scale - portraits use `Math.min(mw/im.naturalWidth, mh/im.naturalHeight)` at lines 3976 and 3996, and the background uses a Math.max cover at line 3934. The dialogue frame is the ONE element in drawCutscene that ignores its source dimensions, and also the only one that skips the X/Y/S plate-space transform set up at line 3921.
- FIX: Replace line 4012 and the blit at 4018-4019 with the same cinPanelRect helper proposed for cinDialogue, so the room/ensemble cutscenes and the per-pilot openings size the panel identically. Do not duplicate the maths a third time.
- risk: drawCutscene's text layout at lines 4057 and 4080-4081 uses DLGW_IN_X/W/Y/H, declared at line 3879 as `const DLGW_IN_X=0.0389, DLGW_IN_W=0.9208, DLGW_IN_Y=0.0817, DLGW_IN_H=1-0.0817*2;`. Those are fractional and survive a change of dw/dh - but the comment block at 3855-3878 states they were measured off dlg_window specifically (and warns at 3870-3874 that the bottom rail is NOT measurable down the centre because of the star medallion), so if the frame key becomes a pilot plate the insets must switch too.

### [CONFIRMED] The ensemble cutscene renderer still hardcodes the bare silver dlg_window - the 0905 'white slab' fix was applied to cinDialogue only
- where: game.js:4018
- now: Lines 4018-4019 ask for `'dlg_window'` unconditionally: `if(typeof XART!=='undefined' && XART.rdy('dlg_window')){ ctx.drawImage(XART.get('dlg_window'), dx, dy, dw, dh); }`. The speaker is not resolved until the NEXT statement, line 4024: `const spk=sc[sc.speaking||'left'];` - and spk.pilot is a valid pilot key, proven by line 4051 which does `const ek='pemb_'+spk.pilot;`. cinDialogue was fixed for exactly this at line 40338 (`const _dk=(_who && XART.rdy('dlg_'+_who)) ? ('dlg_'+_who) : 'dlg_window';`) and its comment at 40327-40337 records Mike's complaint verbatim. The ensemble/room path was never given the same treatment, so every HQ briefing still shows that bare white slab.
- FIX: In drawCutscene, hoist `const spk=sc[sc.speaking||'left'];` from line 4024 to above line 4018, then mirror cinDialogue:40338 - `const _dk=(spk&&spk.pilot&&XART.rdy('dlg_'+spk.pilot))?('dlg_'+spk.pilot):'dlg_window';` - and use _dk in both the rdy() guard on 4018 and the get() on 4019 so the fallback branch at 4020-4022 still fires correctly. Two-line change, no new state.
- risk: The DLGW_IN_* insets used at lines 4057 and 4080-4081 were measured off dlg_window (comment block 3855-3878, constant at 3879). drawCommWindow:51562-51563 already records the correct pair for dedicated plates (0.075 / 0.16 / 0.855 / 0.66) versus generic (0.055 / 0.10 / 0.89 / 0.72), so the same ternary should gate the insets here or the text will sit off on pilot plates. Note the 3879 comment explicitly says drawCommWindow's looser numbers were left alone deliberately, so copying them here is a deliberate widening, not a bug fix.

### [ADJUSTED] cinDialogue applies the GENERIC frame's text insets to dedicated pilot plates
- where: game.js:40340
- now: assets/game.js:40340 is `const padX=dw*.055,nameY=dy+dh*.115,nameH=Math.max(12,Math.round(H*.030));` - the 0.055 is dlg_window's generic inset, used unconditionally even though line 40338 may have selected a dlg_<pilot> plate. drawCommWindow, the reference implementation, explicitly distinguishes the two at assets/game.js:51562-51563: inL uses 0.075 for a dedicated plate versus 0.055 generic, inT 0.16 vs 0.10, inW 0.855 vs 0.89, inH 0.66 vs 0.72 - with the comment at 51561 saying those were measured on the dedicated frame art. Since the 0905 fix made cinDialogue draw pilot plates for the first time, its insets became wrong for the common case: name and body sit ~2% of panel width out over the left rail and the body block is given ~9% more vertical room than the plate has.
- FIX: In cinDialogue, mirror drawCommWindow's ternary. Add `const _ded=(_dk!=='dlg_window');` next to line 40338, then make padX / nameY / bodyY / bodyH read from that: padX=dw*(_ded?0.075:0.055), and scale bodyY/bodyH (currently dh*.34 and dh*.52 at line 40343) off the 0.16/0.66 vs 0.10/0.72 interior instead of the hardcoded fractions. Do not invent new numbers - reuse the four pairs already at 51562-51563. Also re-check the 'NEXT' prompt at line 40349 (`dy+dh*.865`), which is anchored to the same generic interior and will need the same treatment.

### [ADJUSTED] drawCommWindow - the only aspect-correct path - still squashes the generic dlg_window by a magic 0.52
- where: game.js:51528
- now: assets/game.js:51528 is `const ph=_dedicated ? Math.round(pw*(XART.get(_fk).naturalHeight/XART.get(_fk).naturalWidth)) : Math.round(pw*0.52);` with pw fixed at line 51527 as `Math.min(VW-24,446)` = 446 (VW=480, line 14). For a dedicated dlg_<pilot> plate this is genuinely uniform - ph = round(446*321/569) = 252, and 446/252 = 1.770 matches the art's 1.773. This is the correct model and it is why the rival mid-stage dialogue (assets/game.js:55502) and the pilot-select 'GOOD LUCK, PILOT!' window (assets/game.js:51654) look right while the cutscenes do not. But the generic branch uses a hardcoded 0.52 instead of dlg_window's true 808/1465 = 0.5515, giving ph=232 where aspect-correct is 246 - a 5.7% vertical squash on the generic panel. Small, but a real non-uniform scale and the last magic number in the otherwise-clean path.
- FIX: At assets/game.js:51528 replace the _dedicated ternary with an rdy-gated natural ratio, keeping 0.52 only as the pre-decode fallback: `const ph = XART.rdy(_fk) ? Math.round(pw*(XART.get(_fk).naturalHeight/XART.get(_fk).naturalWidth)) : Math.round(pw*0.52);`. Do NOT rely on the rdy check at line 51531 - it runs after this line. One-line change; _dedicated (line 51526) stays as-is because it still gates the drawn border at 51546 and the insets at 51562-51563.
- risk: ph grows from 232 to 246 (+14px) for the generic panel only. py0 is VH*0.34 = 174.08 (line 51529), so the panel bottom moves 406 -> 420 against VH=512. Still clears the screen. The 'PRESS FIRE / ENTER' prompt is drawn at VH*0.34-24 (lines 55505, 55625, 55646), above the panel, so it is unaffected. Dedicated plates are unchanged.

### [CONFIRMED] Source art natural dimensions - where they are declared
- where: manifest.js:4
- now: The dialogue plates are atlas cells, not standalone files. window.BOFX.cells (assets/manifest.js:4) maps key -> [sheet,x,y,w,h] against assets/game/atlas/ui_dialogue.png. Aspects: dlg_window 1465x808 = 1.813; dlg_cole 570x322 = 1.770; dlg_decker 570x321 = 1.776; dlg_axel/freezer/yuri/falva/lizzie 569x321 = 1.773; dlg_juggernaut 552x322 = 1.714; dlg_maverick 552x321 = 1.720. dlg_falva has a standalone override at assets/game.js:1917 which also deletes the cell on 1918; the PNG is 569x321, so the override introduces no aspect change.
- FIX: No code change. Use these as ground truth for the SpriteCook brief: the existing family is ~1.71-1.78:1 for pilot frames and 1.81:1 for the generic. New cinematic windows need a deliberately much wider aspect (see finding 1) and should ship as their own keys (e.g. cin_dlg_<pilot>) rather than replacing dlg_<pilot>, because dlg_<pilot> is shared with pilot select (assets/game.js:51654) and the rival mid-stage dialogue (assets/game.js:55502, 55622, 55643), which are already aspect-correct and must not change shape.

### [ADJUSTED] Other non-uniform blits of the same plate outside the cutscene path (for the 'clean up all frames' sweep)
- where: game.js:39728
- now: The same 6-arg stretch of dlg_window appears on several non-cutscene surfaces, all ignoring the 1.813:1 natural aspect. (a) Arcade pilot-intro 'PILOT DEPLOYED' window, assets/game.js:39728, draws into AIN_BOT declared 39703 as [325,338,302,120] in 640x480 plate space = 2.517:1, a 1.39x horizontal stretch. (b) drawCampSlots, assets/game.js:49959, draws at w=330,h=42 (declared 49950) = 7.857:1, a 4.33x horizontal stretch - the worst in the game. (c) campPauseDraw draws the modal panel itself at 39392 via drawPanel at 464x350 = 1.326:1 (a 0.73x VERTICAL stretch) and its save/load slot buttons at 39411 via drawPanel at bw=424,bh=78 (39379-39381) = 5.44:1, a 3.0x horizontal stretch. (d) In-play dlgBox, assets/game.js:32461, calls drawPanel with pw=344 and ph=Math.round(VH*0.17)=87 (32453-32454) = 3.95:1, a 2.18x stretch. Outside the cutscene complaint, but the same defect, and visible next to any newly-corrected cinematic panel.
- FIX: Out of scope for the cutscene fix - do not touch them in the same change. If the cinPanelRect helper from finding 1 is written generically (takes a max box, returns a uniform-fit rect), these can be converted later one at a time. Note that (c) and (d) route through drawPanel (assets/game.js:39299), so converting them means changing each CALLER's box, not drawPanel itself - drawPanel is a shared primitive used far beyond dlg_window and must keep its literal 6-arg semantics.
- risk: Each has hand-tuned text positions inside the stretched box (e.g. drawAintroPanels sizes its labels off `const inner=dw*0.82` at 39734); changing the frame aspect without re-checking the labels will move text off the panel. Treat as five separate follow-ups, not one sweep. drawCampSlots at 7.86:1 and campPause's slot rows at 5.44:1 are close to the WIDE aspect the new cinematic plates want - if SpriteCook delivers a ~5:1 or ~8:1 variant, those two are better fixed by swapping the art key than by re-fitting the box.

## pacing

### [ADJUSTED] Typewriter speed (the one Mike named): HQ_CPS
- where: game.js:40051
- now: `const HQ_CPS=42;   // characters per second` at line 40051 — the single type-on rate for BOTH cutscene renderers. drawCutsceneBeats line 40562: `hqChars=Math.min(n,hqChars+HQ_CPS*dt)`. drawCutsceneEnsemble line 40626: `hqChars+=HQ_CPS*dt` (guarded by `if(hqChars<n)`). 42 cps is ~500 wpm of streaming text. Longest PILOT_OPENINGS line is 224 chars (line 39824) = 5.3 s; longest HQ_SCENES ensemble line is 118 chars (line 39948) = 2.8 s. The blip SFX rides the same counter — every 3rd character advanced (line 40563, line 40628) — so at 42 cps it ticks 14x/second. Note dt is clamped to 0.05 at line 39494-ish (main loop `dt=Math.max(0,Math.min(dt,0.05)); stateT+=dt;` at line 57494), so the rate is frame-rate independent.
- FIX: Change the literal on line 40051 from 42 to 24. Nothing else needs touching — both call sites already scale by dt, and the every-3rd-character blip drops to ~8/s, closer to a comic panel than the current buzz. 26 is the upper bound if 24 reads sluggish on short lines.
- risk: Low. Both consumers are `x += HQ_CPS*dt` with a clamp; no other code reads HQ_CPS (grep: only lines 40051, 40562, 40626). Only knock-on is blip cadence, which improves.

### [ADJUSTED] Ensemble renderer has NO per-line input lockout — the guard is scene-scoped, not line-scoped
- where: game.js:40649
- now: `if(stateT>0.15 && (Input.mouse.down || anyTap())){` at line 40649 in drawCutsceneEnsemble. stateT is the STATE timer — incremented in the main loop at line 57494 and zeroed only by setState (line 25535); the per-line advance at line 40654 resets hqChars and calls hqSeat but touches no timer. So the 0.15 s protection applies once, to the whole 5-8 line scene. From line 2 onward there is zero minimum display time: two taps 80 ms apart complete the line and advance it. hqBeatT is reset by hqPlay (line 40371) but drawCutsceneEnsemble never increments it — it is dead in this path. This is the single biggest reason an ensemble scene can vanish before it is read.
- FIX: In drawCutsceneEnsemble: add `hqBeatT+=dt;` immediately AFTER the `if(!shown){...return;}` art-ready block (i.e. after line 40608, not after the `if(!ln)` guard at 40582 — otherwise the timer burns while the screen is still black); add `hqBeatT=0;` beside the existing `hqChars=0; hqSeat(hqLine);` on line 40654; and change line 40649's guard from `stateT>0.15` to `hqBeatT>0.55`. Three lines, no new state — hqBeatT already exists and is already zeroed by hqPlay at line 40371.
- risk: Low. hqBeatT is only otherwise read by drawCutsceneBeats (lines 40557, 40568, 40570, 40572) and cinDialogue's NEXT blink (line 40347), neither of which runs in the ensemble path — the two renderers are mutually exclusive via hqMode.

### [ADJUSTED] No dwell/hold after a line finishes typing — in either renderer
- where: game.js:40636
- now: There is no hold constant anywhere in the cutscene code (grep HQ_HOLD: no hits). In the ensemble path the moment `hqChars>=n` the NEXT prompt starts blinking (line 40636) and the very next tap advances (line 40649). In the beats path a dialogue beat is likewise input-only — line 40567's `if(!text&&dur>0)` block is the only auto-advance and it excludes text beats. The game already HAS the constant Mike is asking for, but only for in-mission radio: `const STORY_HOLD = 1.35;` at line 32313, applied at line 32360 as `const wait = (S.when==='safe') ? STORY_HOLD*1.5 : STORY_HOLD;`. The cutscenes never got the equivalent.
- FIX: Add `const HQ_HOLD=0.8;` next to HQ_CPS on line 40051. Gate the advance on time-since-COMPLETE, not time-since-beat-start: add a `hqHoldT` counter that is zeroed whenever hqChars crosses n (including via the snap tap) and incremented per frame once complete. Beats path — in the line 40572 block change `else hqAdvance();` to `else if(hqHoldT>=HQ_HOLD) hqAdvance();`. Ensemble path — in the line 40649 else-branch require `hqHoldT>=HQ_HOLD` before `hqLine++`. The first tap still snaps the text complete instantly, so it never feels unresponsive; it only stops the second tap from eating the line. Do NOT use the report's original `hqBeatT>=(n/HQ_CPS)+HQ_HOLD` form — it punishes the player for snapping.
- risk: Medium. A hold that blocks input reads as a dead button; keep it under 1.0 s and keep the snap tap ungated. Note both blocks end with `Input.mouse.down=false; return;` (line 40573), so a tap rejected by the hold is silently swallowed — consider leaving Input.mouse.down set when the hold rejects, so the advance fires on the next frame rather than needing a fresh press.

### [ADJUSTED] Beats renderer per-beat input lockout: 0.18 s
- where: game.js:40572
- now: Line 40572: `if(hqBeatT>.18&&(Input.mouse.down||anyTap())){` followed on line 40573 by `if(text&&hqChars<n)hqChars=n; else hqAdvance(); Input.mouse.down=false; return;`. Unlike the ensemble path this one IS per-beat — hqAdvance zeroes hqBeatT at line 40364, and hqBeatT is incremented at line 40555 (correctly placed after the `if(!cinWarm(hqSc))return;` art gate on line 40554). But 0.18 s is far too short to survive a player still mashing out of a stage transition: two taps inside a third of a second take a whole beat.
- FIX: Raise the `.18` literal on line 40572 to `.55`. One number.
- risk: Low. Self-contained literal; hqBeatT is already correctly per-beat here.

### [ADJUSTED] Auto-advance vs wait-for-input: the split is by beat type, and it is undocumented
- where: game.js:40566
- now: Line 40566: `const dur=beat.duration||0;` then line 40567: `if(!text&&dur>0){ ... if(hqBeatT>=dur){hqAdvance();return;} }`. So in the pilot-opening path: a beat WITH text waits for input forever (no timeout path exists for it); a beat with NO text auto-advances on its `duration`. No beat in PILOT_OPENINGS carries both fields. The ensemble path (HQ_SCENES, line 39918) has no duration field at all and is 100% wait-for-input. Net effect on Mike's complaint: the dialogue is not what auto-runs — it is the action frames (the shootdowns, the approaches) that flick past, plus the campaign intro and the ending, which are pure timeline.
- FIX: No change to the mechanism — wait-for-input on dialogue is correct and should stay. Report this to Mike as the answer to 'auto-advance vs wait-for-input': dialogue holds, action frames do not, and the action frames are the short ones.
- risk: None — no edit proposed.

### [ADJUSTED] Action-beat durations — the frames Mike wants to see the enemies in
- where: game.js:39822
- now: Every text-free beat in PILOT_OPENINGS with its `duration` in seconds: aftermath 1.55 (lines 39822, 39829); ram 1.75 (39865); cloak 1.75 (39882); barrel 1.85 (39839); joyride 1.95 (39858); duo_attack 2.10 (39844, 39873); dogfight 2.15 (39837); missile_lock 2.35 (39852); jungle_approach 3.15 (39834, 39849, 39879); hq_approach 3.75 (39826, 39841, 39855, 39861, 39870, 39889). Each span is also the easing window — cinDrawMotion reads `beat.duration` for its cinClose smoothstep at lines 40221, 40251, 40260, 40268, 40281, 40299, 40307 and 40315 — so the hostile closing on camera, the airburst timing and the approach scaling all complete inside it. The shortest, the 1.55 s aftermath at line 39822, is the very first frame of the campaign and the one meant to sell 'what just happened to us'.
- FIX: Multiply the combat/aftermath durations by ~1.6 in the data table only: line 39822 and 39829 aftermath 1.55->2.6; line 39865 ram 1.75->2.8; line 39882 cloak 1.75->2.6; line 39839 barrel 1.85->2.8; line 39858 joyride 1.95->2.8; lines 39844 and 39873 duo_attack 2.10->3.4; line 39837 dogfight 2.15->3.4; line 39852 missile_lock 2.35->3.6. Leave the approach beats (3.15 / 3.75) alone — already the long ones, and they land on HQ. cinClose easing follows automatically because it reads beat.duration.
- risk: The hardcoded fallbacks in cinDrawMotion (line 40251 `||2.15`, line 40260 `||2.10`, line 40268 `||2.30`, line 40281 `||1.90`, line 40299 `||3.75`, line 40307 `||3.15`, line 40315 `||2.65`) still carry the OLD values, but they only fire when duration is absent — the dialogue-beat case covered in the next finding. Worth updating in the same pass for consistency, not required.

### [ADJUSTED] During a dialogue beat the ships freeze — the motion easing has no duration to run against
- where: game.js:40221
- now: Line 40221 in cinFormation: `const near=cinClose(t,beat.duration||2.4,0,1).e;   // 0..1 across the beat, smoothstepped`. A dialogue beat has no `duration` field, so the easing uses the 2.4 s fallback and cinClose clamps `u=clamp(t/Math.max(0.01,dur||2.0),0,1)` (line 40196) at 1. A wait-for-input dialogue beat routinely sits on screen 6-12 s; after 2.4 s the `near`-driven cues — the speaker's push toward camera (line 40236, `+0.09*near`), the listener's ease back (`-0.035*near`), the horizontal separation (line 40238) and the vertical placement that follows z — are all pinned at their end value. The per-ship sine breathe, the horizontal drift and the 5px bob keep running, so the frame is stale rather than frozen. The same clamp affects the dogfight/duo_attack/missile_lock/ram fallbacks at lines 40251, 40260, 40268, 40281.
- FIX: In cinFormation line 40221, when the beat has no duration, drive the depth from a looping phase rather than a clamped ramp: `const near = beat.duration ? cinClose(t,beat.duration,0,1).e : 0.5+0.5*Math.sin(t*0.42);`. One expression, no new art, no data change.
- risk: Only affects text beats (the ones with no duration); duration-bearing action beats keep the exact easing they have. Note this makes the two ships slowly swap which is nearer, which changes the plan.sort draw order on line 40241 mid-beat — acceptable for a slow 15 s cycle, but check it does not pop.

### [ADJUSTED] Fade between beats — 0.24 s in / 0.34 s out, and ONLY on text-free beats
- where: game.js:40568
- now: Line 40568: `const fadeIn=clamp(1-hqBeatT/.24,0,1),fadeOut=clamp((hqBeatT-(dur-.34))/.34,0,1),fa=Math.max(fadeIn,fadeOut);` then line 40569 paints `rgba(0,0,0,fa)` over the full frame. It sits inside the `if(!text&&dur>0)` block opened on line 40567, so it runs on action beats only: every DIALOGUE beat cuts hard, with no transition whatsoever. The letterbox bars on action beats (line 40559, `Math.max(10,Math.round(H*.052))`) also pop on and off instantly with no ramp.
- FIX: Lengthen to `.40` in and `.50` out on line 40568, and lift the fade out of the `!text` block so dialogue beats get the same 0.40 s fade-in — move the fadeIn computation above the line 40567 gate and draw it unconditionally, keeping fadeOut inside the gate (a wait-for-input beat has no known end to fade toward).
- risk: Moving the fade above the gate means it now paints on dialogue beats; it must draw AFTER cinDialogue (line 40564) or the black will sit under the panel instead of over it. Current order is backdrop -> motion -> bars -> dialogue -> fade, so keep the draw at line 40568's position and only move the `!text` gating, not the paint.

### [ADJUSTED] No transition at all between ensemble lines
- where: game.js:40654
- now: Line 40654: `hqChars=0; hqSeat(hqLine);` — the entire line change. No fade, no panel wipe, no beat. drawCutsceneEnsemble (lines 40577-40659) contains no fade or transition constant of any kind; the portrait swaps and the text restarts on the same frame.
- FIX: With hqBeatT wired up (finding 2), gate a short black ramp on it: after line 40603's `const shown = drawCutscene(sc);` and its `if(!shown)` early-return block (so after line 40608), add `const _f=clamp(1-hqBeatT/0.30,0,1); if(_f>0){ctx.fillStyle='rgba(0,0,0,'+_f.toFixed(3)+')';ctx.fillRect(0,0,_cutW,VH);}`. Two lines. Depends on finding 2 landing first, with hqBeatT incremented after the art gate as corrected there.
- risk: Purely additive draw; worst case is a flash if hqBeatT is not actually reset per line. Because it must sit after the `if(!shown)` return at line 40608, the ramp will not paint while art is still decoding — which is correct, that hold is already black.

### [ADJUSTED] Campaign intro is a hard timeline with no wait-for-input — the worst reading rate in the build
- where: game.js:40413
- now: CAMPAIGN_INTRO_BEATS (lines 40413-40428) is seven beats with absolute `at`/`to` seconds: 0->3.5, 3.5->6.8, 6.8->10.1, 10.1->13.6, 13.6->17.0, 17.0->20.6, 20.6->24.6. Then `const CAMPAIGN_INTRO_FINALE=24.6, CAMPAIGN_INTRO_DONE=28.7;` on line 40429. drawCampaignIntro (lines 40518-40545) picks the beat by wall-clock t at line 40532; input at line 40542 (`if(stateT>.35&&(click||anyTap())){...campaignIntroFinish();return;}`) only SKIPS the whole thing, it never holds. Caption alpha ramps in over 0.28 s and out over 0.32 s (line 40538, `const ca=Math.min(clamp(local/.28,0,1),clamp((B.to-t)/.32,0,1));`), so a 3.3 s beat gives ~2.7 s of readable time. Body lengths measured from the table: 72, 118, 124, 104, 101, 123, 125 characters — plus a heading each. Beat 3 is 124 chars in 2.7 s = 46 characters per second of reading demand; comfortable is 14-17. Roughly 3x too fast with no way to pause.
- FIX: Minimal: rescale the table on lines 40413-40429 by ~2.2x, keeping the beats contiguous — at/to become 0, 7.7, 15.0, 22.3, 30.0, 37.5, 45.4, 54.2, with CAMPAIGN_INTRO_FINALE=54.2 and CAMPAIGN_INTRO_DONE=58.3 on line 40429. Only the numbers change; drawCampaignIntro reads them and needs no edit. Better and what I would actually recommend to Mike, since a 54 s clock-paced intro is its own problem: give this screen the same type-on plus wait-for-input the cutscenes have, so it is paced by the reader — but that is a behaviour change, not a constant change, so flagging rather than proposing.
- risk: CAMPAIGN_INTRO_FINALE and CAMPAIGN_INTRO_DONE (line 40429) must move with the table or the last beat is cut off / the finale plays over it. campaignIntroFinale's own offsets (fireAt=.62 and step=.13 on line 40505, and the 1.18/1.45/3.18 ramps on lines 40514 and 40516) are relative to `q=t-FINALE` and must NOT be rescaled. Also note line 40539's scene-open fade `clamp(1-t/.42,0,1)` is absolute-t and stays correct either way.

### [ADJUSTED] Campaign intro crossfade and caption ramps
- where: game.js:40533
- now: Line 40533: `const B=CAMPAIGN_INTRO_BEATS[i],local=t-B.at,fade=.42;` — a 0.42 s crossfade into the next plate, drawn at lines 40535-40537 (`if(B.to-t<fade&&i+1<CAMPAIGN_INTRO_BEATS.length){ const na=clamp((t-(B.to-fade))/fade,0,1); campaignIntroVisual(CAMPAIGN_INTRO_BEATS[i+1],0,W,H,na); }`), plus the caption's own 0.28 s in / 0.32 s out on line 40538. The caption fade is nested INSIDE the beat window, so it eats 0.6 s off every already-too-short beat.
- FIX: Raise `fade=.42` to `.65` on line 40533 for a softer plate change. Leave the 0.28/0.32 caption ramps on line 40538 alone once the beat windows are lengthened — at 7 s beats they cost proportionally nothing.
- risk: Low. At the current 3.3 s beats a 0.65 s crossfade would overlap 20% of a beat; only raise it in the same pass as finding 10's rescale, not before.

### [ADJUSTED] Victory/ending cinematic act boundaries
- where: game.js:57304
- now: Lines 57304-57305: `const VICTORY_DISH_AT=8.4, VICTORY_WARNING_AT=15.8, VICTORY_SPACE_AT=22.8,` / `VICTORY_STINGER_AT=29.3, VICTORY_CARD_AT=32.6;` — absolute seconds against drawVictory._t, dispatched at lines 57456-57461. Pure timeline: the only input in the whole function is the return-to-title at line 57463, gated on `t>VICTORY_CARD_AT+5`. Nothing in the ending can be held, skipped or advanced by the player. Act lengths are 8.4 / 7.4 / 7.0 / 6.5 / 3.3 s.
- FIX: The act lengths themselves are defensible; the captions inside them are not (next finding). If Mike wants the ending slower overall, scale all five on lines 57304-57305 by 1.25: 10.5, 19.8, 28.5, 36.6, 40.8 — but scale the caption windows in the same pass or the text just sits longer against a finished animation.
- risk: The per-act functions key off `q = t - <ACT>_AT` and carry their own internal offsets (victoryDish's 4.75/4.25/6.75 at lines 57358/57367/57374; victoryStinger's from line 57407); those are act-relative and must NOT be scaled, only the five boundary constants.

### [ADJUSTED] Victory caption windows — same too-fast problem as the campaign intro
- where: game.js:57383
- now: Three hardcoded caption windows, all with 0.28-0.35 s alpha ramps eaten out of them. victoryWarning line 57383: `const second=q>=3.15,local=second?q-3.15:q,dur=second?3.85:3.15;` with the alpha on line 57384 — the Space Federation line is 104 chars in 3.85 s (~3.2 s visible = 32 c/s). victoryDish line 57369: `if(q<4.55){` with alpha `Math.min(clamp(q/.32,0,1),clamp((4.55-q)/.38,0,1))` on line 57370, for a 69-char body plus heading. victorySpaceLaunch line 57400: `if(q<2.7)victoryCaption(...,Math.min(clamp(q/.3,0,1),clamp((2.7-q)/.35,0,1)));` for a 56-char body, leaving ~2.05 s. The pilot's own ending caption (line 57352, `const a=Math.min(clamp(t/.34,0,1),clamp((VICTORY_DISH_AT-t)/.48,0,1));`) is the one that gets fair time — it runs the full 8.4 s of act one.
- FIX: Line 57383: change `3.15` to `4.6` in both places and `3.85` to `5.6`, AND raise VICTORY_SPACE_AT from 22.8 to 26.0 (not 25.0) on line 57304, so the 10.2 s of caption fits inside the 10.2 s act. Line 57400: change both `2.7` literals to `4.0` (act is 6.5 s, fits). Line 57369: change `4.55` to `5.6` in both places (act is 7.4 s and the cut fade starts at q=6.75 on line 57374, so 5.6 fits). All are literal swaps inside their own act.
- risk: Each window must stay inside its act's length or the caption is clipped by the next act taking over — verify each against lines 57304-57305 after editing, and note raising VICTORY_SPACE_AT to 26.0 pushes every later boundary unless VICTORY_STINGER_AT and VICTORY_CARD_AT move with it (otherwise victorySpaceLaunch's act shrinks from 6.5 s to 3.3 s and its own 5.55 s music fade on line 57402 never fires).

### [ADJUSTED] NEXT prompt blink rate (minor)
- where: game.js:40636
- now: Line 40636 in drawCutsceneEnsemble: `if(hqChars>=n && Math.floor(stateT*2.2)%2){`, and the near-identical `Math.floor(hqBeatT*2.2)%2` in cinDialogue at line 40347. 2.2 gives a 1.1 Hz blink. The ensemble one keys off stateT, which is zeroed only by setState (line 25535), so across a scene the blink phase is continuous rather than restarting per line — cosmetically fine, but it means the prompt can appear mid-blink-off the instant a line completes.
- FIX: Leave the rate. If finding 2 lands, change line 40636's `stateT` to `hqBeatT` so the prompt always fades up cleanly on the ON half when a line finishes. One identifier.
- risk: Low, but it is strictly dependent on finding 2 — hqBeatT is currently never incremented in drawCutsceneEnsemble, so making this swap alone would freeze the prompt permanently at whatever `Math.floor(0*2.2)%2` gives (0, i.e. invisible).
