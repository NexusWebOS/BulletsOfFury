# Stage 5 Archmage core recovery — September 16, 2026

Destroying the Chrome Hammer Archmage's chaingun on Easy or Normal no longer strands the boss in the unimplemented `core_orbit` state. The final modular break now retires both old module targets, cancels any detached hammer, and begins a finite 2.15-second core conversion. The boss returns to its fighting anchor while three authored energy cells accelerate around its blue-palette break reel and converge into the chest core with charge and phase-change sounds.

The conversion releases into the existing authored dual-Uzi final form, which advances through Uzi pressure, the fused mega-wave and back into Uzi pressure instead of freezing. The 1.65-second mega-wave charge now renders the shared green/yellow/red corridor over its exact damage width before the beam becomes active. Hard and Furious retain their separate red enrage entrance and later attack routing.

The final break also marks the obsolete hammer module destroyed. This prevents an invisible old hardpoint from absorbing later shots and redirecting the final form back into the already-destroyed chaingun phase.

Focused section 358 passes **10/10**. The full suite reaches its final summary with **4,530 passing assertions / 57 failures**: the established 56-name baseline plus the known intermittent Stage-1 sand-tank fixture, with no new failure name. Real Chromium passes **22/22** with zero page, console or game-loop errors. Nine native 960×1024 frames were inspected across the blue core orbit, convergence, dual-Uzi release, green/yellow/red mega-wave warning, beam release and return to Uzi pressure.

Machine-readable proof: [qa/archmage_core_recovery_0916.json](qa/archmage_core_recovery_0916.json).
